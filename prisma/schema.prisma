generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = "mysql://root:qUA@M2iyNA_LLaEG-@gRA-_@35.184.135.162:3306/blb?schema=public?connect_timeout=300"
}

model FawryPayment {
  Id                Int       @id @unique(map: "Id_UNIQUE") @default(autoincrement())
  referenceNumber   String    @unique(map: "referenceNumber_UNIQUE") @db.VarChar(300)
  merchantRefNumber String?   @db.VarChar(300)
  orderAmount       String?   @db.VarChar(45)
  paymentAmount     String?   @db.VarChar(45)
  fawryFees         String?   @db.VarChar(45)
  orderStatus       String?   @db.VarChar(45)
  paymentMethod     String?   @db.VarChar(45)
  expirationTime    DateTime? @db.DateTime(0)
  customerName      String?   @db.VarChar(300)
  customerMobile    String?   @db.VarChar(300)
  customerMail      String?   @db.VarChar(300)
  customerProfileId String?   @db.VarChar(300)
  signature         String?   @db.VarChar(255)
  taxes             String?   @db.VarChar(45)
  statusCode        String?   @db.VarChar(45)
  statusDescription String?   @db.VarChar(255)
  basketPayment     String?   @db.VarChar(45)
  itemId            String?   @db.VarChar(300)
  currencyCode      String?   @db.VarChar(45)
  description       String?   @db.VarChar(255)
  type              String?   @db.VarChar(45)
  paymentTime       String?   @db.VarChar(100)
  requestId         String?   @unique(map: "requestId_UNIQUE") @db.VarChar(100)
  createdAt         DateTime? @default(now()) @db.Timestamp(0)
  paymentId         Int?
  payments          payments? @relation(fields: [paymentId], references: [id], onDelete: Cascade, map: "fk_payment_paymentId")

  @@index([paymentId], map: "fk_paymets_fawry_idx")
}

model Grades_Payment_system {
  Id    Int     @id @unique(map: "idGrades_Payment_system_UNIQUE") @default(autoincrement())
  Name  String? @unique(map: "Name_UNIQUE") @db.VarChar(45)
  Price String? @db.VarChar(45)
}

model activities {
  id                       Int                       @id @default(autoincrement())
  title                    String?                   @db.VarChar(255)
  type                     String?                   @db.VarChar(255)
  link                     String?                   @db.VarChar(700)
  file_name                String?                   @db.VarChar(500)
  order                    Int?
  lesson                   Int?
  ref                      Int?
  questions                Int?
  standard                 Int?
  price_type               String?                   @db.VarChar(255)
  price                    Int?
  description              String?                   @db.VarChar(1000)
  fawry                    Int?                      @default(0)
  fawry_price              Int?
  views                    Int?                      @default(0)
  vimeo_id                 String?                   @db.VarChar(900)
  created_at               DateTime?                 @default(now()) @db.Timestamp(0)
  updated_at               DateTime?                 @default(now()) @db.Timestamp(0)
  downloadable             Int?                      @default(0) @db.TinyInt
  is_online                Int?                      @default(0) @db.TinyInt
  online_price             Int?
  subject                  Int?
  duration                 Int?                      @default(0)
  online_enroll_duration   Int?                      @default(0)
  percentage_of_attendance Int?
  live_session             Int?                      @default(0) @db.TinyInt
  bunny_video_id           String?                   @db.VarChar(100)
  video_source_path        String?                   @db.VarChar(400)
  bunny_library_id         Int?
  bunny_collection_id      String?                   @db.VarChar(100)
  time                     Int?
  lessons                  lessons?                  @relation(fields: [lesson], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_activities_lessons1")
  subjects                 subjects?                 @relation(fields: [subject], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_activities_subject1")
  activities_comments      activities_comments[]
  activities_has_students  activities_has_students[]
  activities_likes         activities_likes[]
  activities_progress      activities_progress[]
  advertise_details        advertise_details[]
  coupons                  coupons[]
  point_configurations     point_configurations[]
  resources                resources[]
  sequences_lessons        sequences_lessons[]
  sessions                 sessions[]

  @@index([lesson], map: "fk_activities_lessons1_idx")
  @@index([subject], map: "fk_activities_subject1_idx")
}

model activities_has_students {
  activities_id Int
  students_id   Int
  count         Int?       @default(1)
  watch_time    Int?       @default(0)
  created_at    DateTime?  @default(now()) @db.DateTime(0)
  updated_at    DateTime?  @default(now()) @db.DateTime(0)
  activities    activities @relation(fields: [activities_id], references: [id], onDelete: Cascade, map: "fk_activities_has_students_activities1")
  students      students   @relation(fields: [students_id], references: [id], onDelete: Cascade, map: "fk_activities_has_students_students1")

  @@id([activities_id, students_id])
  @@index([activities_id], map: "fk_activities_has_students_activities1_idx")
  @@index([students_id], map: "fk_activities_has_students_students1_idx")
}

model activities_progress {
  students_id   Int
  activities_id Int
  finish        Int?       @default(0) @db.TinyInt
  created_date  DateTime?  @default(now()) @db.DateTime(0)
  activities    activities @relation(fields: [activities_id], references: [id], onDelete: Cascade, map: "fk_activities_progress_activities1")
  students      students   @relation(fields: [students_id], references: [id], onDelete: Cascade, map: "fk_activities_progress_students1")

  @@id([students_id, activities_id])
  @@index([activities_id], map: "fk_activities_progress_activities1_idx")
  @@index([students_id], map: "fk_students_has_activities_students1_idx")
}

model admission_status {
  id          Int           @id @default(autoincrement())
  title       String?       @db.VarChar(255)
  type        String?       @db.VarChar(255)
  application application[]
}

model ads {
  id             Int              @id @unique(map: "id_UNIQUE") @default(autoincrement())
  title          String           @db.VarChar(128)
  description    String           @db.VarChar(255)
  image_url      String           @db.VarChar(300)
  url            String           @db.VarChar(255)
  views          Int              @default(0)
  teacher_id     Int?
  active         Int?             @default(1)
  share          Int?
  subjects_id    Int?
  is_general     Int?             @default(0) @db.TinyInt
  subjects       subjects?        @relation(fields: [subjects_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_ads_subjects1")
  teachers       teachers?        @relation(fields: [teacher_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_ads_teachers1")
  ads_has_grades ads_has_grades[]

  @@index([subjects_id], map: "fk_ads_subjects1_idx")
  @@index([teacher_id], map: "fk_ads_teachers1_idx")
}

model ads_has_grades {
  ads_id    Int
  grades_id Int
  ads       ads    @relation(fields: [ads_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_ads_has_grades_ads1")
  grades    grades @relation(fields: [grades_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_ads_has_grades_grades1")

  @@id([ads_id, grades_id])
  @@index([ads_id], map: "fk_ads_has_grades_ads1_idx")
  @@index([grades_id], map: "fk_ads_has_grades_grades1_idx")
}

model answers {
  id             Int       @id @default(autoincrement())
  student        Int?
  activity_type  String?   @db.VarChar(100)
  activity_id    Int?
  question       Int?
  answer         String?   @db.VarChar(20)
  bloom          Int?
  dok            Int?
  difficulty     Int?
  student_answer String?   @db.VarChar(255)
  answer_date    DateTime? @db.DateTime(6)
}

model application {
  id                Int              @id @default(autoincrement())
  workflow          Int?
  current_step      Int?
  firstname         String?          @db.VarChar(255)
  middlename        String?          @db.VarChar(255)
  lastname          String?          @db.VarChar(255)
  application_date  String?          @db.VarChar(255)
  application_time  String?          @db.VarChar(255)
  has_workflow      Int?
  gender            String?          @db.VarChar(255)
  secondlanguage    String?          @db.VarChar(255)
  religion          String?          @db.VarChar(255)
  phone             String?          @db.VarChar(255)
  mobile            String?          @db.VarChar(255)
  citizenship       String?          @db.VarChar(255)
  branch_id         Int
  grade_id          Int
  classroom_id      Int?
  admission_date    String?          @db.VarChar(255)
  admission_time    String?          @db.VarChar(255)
  date_of_birth     String?          @db.VarChar(255)
  image             String?          @db.VarChar(255)
  address           String?          @db.VarChar(255)
  preschool         String?          @db.VarChar(255)
  father_mobile     String?          @db.VarChar(255)
  father_email      String?          @db.VarChar(255)
  father_degree     String?          @db.VarChar(255)
  father_company    String?          @db.VarChar(255)
  father_occupation String?          @db.VarChar(255)
  mother_name       String?          @db.VarChar(255)
  mother_mobile     String?          @db.VarChar(255)
  mother_email      String?          @db.VarChar(255)
  mother_degree     String?          @db.VarChar(255)
  mother_company    String?          @db.VarChar(255)
  mother_occupation String?          @db.VarChar(255)
  previous_school   String?          @db.VarChar(255)
  havesibling       String?          @db.VarChar(255)
  parentsEmployee   String?          @db.VarChar(255)
  status_id         Int
  branches          branches         @relation(fields: [branch_id], references: [id])
  grades            grades           @relation(fields: [grade_id], references: [id])
  admission_status  admission_status @relation(fields: [status_id], references: [id])

  @@index([branch_id], map: "application_branch_id_fkey")
  @@index([grade_id], map: "application_grade_id_fkey")
  @@index([status_id], map: "application_status_id_fkey")
}

model ashtar_codes {
  id                  Int                  @id @default(autoincrement())
  code                String?              @db.VarChar(50)
  title               String?              @db.VarChar(255)
  grade               Int?
  value               Decimal?             @db.Decimal(10, 0)
  used                Boolean?             @default(false)
  owner               Int?
  issued_date         DateTime?            @db.DateTime(6)
  expiry_date         DateTime?            @db.DateTime(6)
  redeem_date         DateTime?            @db.DateTime(6)
  teacher             Int?
  comment             String?              @db.VarChar(255)
  active              Int                  @default(1) @db.TinyInt
  expiry_days         Int?
  created_at          DateTime?            @default(now()) @db.DateTime(0)
  admin_id            Int?
  discount            Int?
  super_user          super_user?          @relation(fields: [admin_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_admin_codes")
  grades              grades?              @relation(fields: [grade], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_ashtar_codes_grade")
  students            students?            @relation(fields: [owner], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_ashtar_codes_students1")
  teachers            teachers?            @relation(fields: [teacher], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_ashtar_codes_teachers1")
  family_subscription family_subscription? @relation(fields: [discount], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_family_subscription")
  paymob_codes        paymob_codes[]

  @@index([owner], map: "fk_ashtar_codes_students1_idx")
  @@index([teacher], map: "fk_ashtar_codes_teachers1_idx")
  @@index([discount], map: "fk_family_subscription_idx")
  @@index([admin_id], map: "fk_admin_idx")
  @@index([grade], map: "fk_ashtar_codes_grades1_idx")
}

model ashtar_enrollments {
  id                   Int                @id @unique(map: "id_UNIQUE") @default(autoincrement())
  student              Int?
  grade                Int?
  code                 String?            @db.VarChar(50)
  enrollment_date      DateTime?          @db.DateTime(0)
  points               Int?
  active               Boolean?           @default(true)
  balance              Int?
  group                Int?
  comments             String?            @db.VarChar(500)
  old_student          Int?
  terms_id             Int?
  transactions_id      Int
  payment_types        String             @default("codes") @db.VarChar(50)
  question_bank        Int?               @default(0) @db.TinyInt
  subject_id           Int?
  expiry_date          DateTime?          @db.DateTime(0)
  live_session         Int?               @default(0) @db.TinyInt
  zaker                Int?               @default(0) @db.TinyInt
  question_bank_enroll Int?               @default(0) @db.TinyInt
  grades               grades?            @relation(fields: [grade], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_grades_id")
  subjects             subjects?          @relation(fields: [subject_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_subjects_id")
  terms                terms?             @relation(fields: [terms_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_ashtar_enrollments_terms1")
  zaker_transactions   zaker_transactions @relation(fields: [transactions_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_ashtar_enrollments_zaker_transactions1")
  students             students?          @relation(fields: [student], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "subscribers")

  @@index([student], map: "subscribers")
  @@index([grade], map: "teacher_id")
  @@index([terms_id], map: "fk_ashtar_enrollments_terms1_idx")
  @@index([transactions_id], map: "fk_ashtar_enrollments_zaker_transactions1_idx")
  @@index([subject_id], map: "fk_ashtar_enrollments_subjects1_idx")
}

model assignments {
  id                        Int                         @id @default(autoincrement())
  title                     String?                     @db.VarChar(255)
  type                      String?                     @default("Digitized Assignment") @db.VarChar(100)
  lesson                    Int?
  grade                     Int?
  image                     String?                     @db.VarChar(255)
  video                     String?                     @db.VarChar(255)
  sound                     String?                     @db.VarChar(255)
  link                      String?                     @db.VarChar(255)
  weight                    Int?                        @default(125)
  difficulty                Int?
  bloom                     Int?
  dok                       Int?
  duration                  Int?
  questions_count           Int?
  attempts                  Int?
  tags                      String?                     @db.VarChar(255)
  open_date                 String?                     @db.VarChar(100)
  close_date                String?                     @db.VarChar(100)
  cutoff_date               String?                     @db.VarChar(100)
  extend_date               String?                     @db.VarChar(100)
  non_graded                Boolean?
  description               String?                     @db.VarChar(255)
  questions_type            assignments_questions_type? @default(Random)
  active                    Int?                        @default(1) @db.TinyInt
  is_quiz                   Int?                        @default(0) @db.TinyInt
  grade_pass                Int?                        @default(60)
  show_answers              Int?                        @default(0) @db.TinyInt
  updated_at                DateTime?                   @default(now()) @db.DateTime(0)
  created_at                DateTime?                   @default(now()) @db.DateTime(0)
  manual_grade              Int?                        @default(0) @db.TinyInt
  subject                   Int?
  price                     Int?                        @default(0)
  is_online                 Int?                        @default(0)
  online_price              Int?                        @default(0)
  lessons                   lessons?                    @relation(fields: [lesson], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_assignments_lessons1")
  subjects                  subjects?                   @relation(fields: [subject], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_assingment_subject")
  weights                   weights?                    @relation(fields: [weight], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "weight")
  assignments_has_questions assignments_has_questions[]
  buyers                    buyers[]
  coupons                   coupons[]
  exams                     exams[]
  point_configurations      point_configurations[]
  resources                 resources[]
  sequences_lessons         sequences_lessons[]
  submissions               submissions[]

  @@index([lesson], map: "fk_assignments_lessons1_idx")
  @@index([subject], map: "fk_assingment_subject_idx")
  @@index([weight], map: "wiegthsfk_idx")
}

model assignments_has_questions {
  assignments_id Int
  questions_id   Int
  grade          Int?
  assignments    assignments @relation(fields: [assignments_id], references: [id], onDelete: Cascade, map: "fk_assignments_has_questions_assignments1")
  questions      questions   @relation(fields: [questions_id], references: [id], onDelete: Cascade, map: "fk_assignments_has_questions_questions1")

  @@id([assignments_id, questions_id])
  @@index([assignments_id], map: "fk_assignments_has_questions_assignments1_idx")
  @@index([questions_id], map: "fk_assignments_has_questions_questions1_idx")
}

model attendance {
  students_id       Int?
  lessons_id        Int?
  id                Int                           @id @default(autoincrement())
  score             Int?
  type              attendance_type?              @default(attend)
  sessions_id       Int?
  attendance_method attendance_attendance_method? @default(manual)
  recorded_at       DateTime?                     @default(now()) @db.Timestamp(0)
  note              String?                       @db.VarChar(400)
  sessions          sessions?                     @relation(fields: [sessions_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_attendance_sessions1")
  lessons           lessons?                      @relation(fields: [lessons_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_students_has_lessons_lessons1")
  students          students?                     @relation(fields: [students_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_students_has_lessons_students1")

  @@index([sessions_id], map: "fk_attendance_sessions1_idx")
  @@index([lessons_id], map: "fk_students_has_lessons_lessons1_idx")
  @@index([students_id], map: "fk_students_has_lessons_students1_idx")
}

model branches {
  id                         Int                          @id @default(autoincrement())
  title                      String?                      @db.VarChar(255)
  interval                   Int?
  days_off                   String?                      @db.VarChar(255)
  icon                       String?                      @db.VarChar(255)
  admission                  Int?
  season                     Int?
  admission_open_date        String?                      @db.VarChar(255)
  admission_close_date       String?                      @db.VarChar(255)
  admission_open_time        String?                      @db.VarChar(255)
  admission_close_time       String?                      @db.VarChar(255)
  showed_title               String?                      @db.VarChar(75)
  old_branches               Int?
  active                     Int?                         @default(1) @db.TinyInt
  image                      String?                      @default("https://storage.googleapis.com/nis_control_bucket/branches/branchsvg.svg") @db.VarChar(250)
  description                String?                      @db.VarChar(250)
  selectedCategoryFirst      Int?                         @default(0) @db.TinyInt
  next_step                  Int?
  number_of_terms            Int?                         @default(2)
  language                   String?                      @default("ar") @db.VarChar(45)
  application                application[]
  branch_countries           branch_countries[]
  onboarding_steps           onboarding_steps?            @relation(fields: [next_step], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "next_step_branch_id")
  category                   category[]
  devision                   devision[]
  grades                     grades[]
  modarsina_mirror           modarsina_mirror[]
  students                   students[]
  subjects                   subjects[]
  teachers_specific_branches teachers_specific_branches[]
  terms                      terms[]
  zaker                      zaker[]

  @@index([next_step], map: "next_step_branch_id_idx")
}

model breakdowns {
  id     Int     @id @default(autoincrement())
  school Int?
  title  String? @db.VarChar(255)
  order  Int?
}

model buyers {
  id                          Int                        @id @default(autoincrement())
  student                     Int?
  subject                     Int?
  teacher                     Int?
  code                        String?                    @db.VarChar(50)
  enrollment_date             DateTime?                  @db.DateTime(0)
  points                      Int?
  active                      Boolean?                   @default(true)
  balance                     Int?
  group                       Int?
  activity                    String?                    @db.VarChar(255)
  activity_id                 Int?
  comments                    String?                    @db.VarChar(500)
  section_id                  Int?
  lesson_id                   Int?
  type                        String?                    @db.VarChar(45)
  duration                    Int?
  expired_date                DateTime?                  @db.DateTime(0)
  transactions_our_teacher_id Int?
  assignment_id               Int?
  subjects                    subjects?                  @relation(fields: [subject], references: [id], onDelete: Cascade, map: "buyers_ibfk_1")
  teachers                    teachers?                  @relation(fields: [teacher], references: [id], onDelete: Cascade, map: "buyers_ibfk_2")
  assignments                 assignments?               @relation(fields: [assignment_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_assigmnet_buyer")
  students                    students?                  @relation(fields: [student], references: [id], onDelete: Cascade, map: "fk_buyers_students1")
  transactions_our_teachers   transactions_our_teachers? @relation(fields: [transactions_our_teacher_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_transaction_buyers")

  @@index([subject], map: "subject")
  @@index([teacher], map: "teacher_id")
  @@index([student], map: "fk_buyers_students1_idx")
  @@index([assignment_id], map: "fk_assigmnet_buyer_idx")
  @@index([code], map: "fk_code_buyers")
  @@index([transactions_our_teacher_id], map: "fk_transaction_buyers_idx")
}

model calendars {
  id       String  @id @db.VarChar(90)
  name     String? @db.VarChar(45)
  location String? @db.VarChar(90)
  rooms    rooms[]
}

model category {
  id               Int               @id @unique(map: "id_UNIQUE") @default(autoincrement())
  name             String?           @db.VarChar(45)
  branch_id        Int?
  active           Int?              @default(1) @db.TinyInt
  old_category     Int?
  next_step        Int?
  showed_title     String?           @db.VarChar(45)
  branches         branches?         @relation(fields: [branch_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_category_branches1")
  onboarding_steps onboarding_steps? @relation(fields: [next_step], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fks_next_step")
  grades           grades[]

  @@index([branch_id], map: "fk_category_branches1_idx")
  @@index([next_step], map: "fks_next_step_idx")
}

model classrooms {
  id       Int        @id @default(autoincrement())
  title    String?    @db.VarChar(255)
  room     String?    @db.VarChar(255)
  capacity Int?
  grade_id Int
  grades   grades     @relation(fields: [grade_id], references: [id])
  students students[]

  @@index([grade_id], map: "classrooms_grade_id_fkey")
}

model clients {
  id           Int        @id @default(autoincrement())
  firstname    String?    @db.VarChar(255)
  lastname     String?    @db.VarChar(255)
  phone        String?    @db.VarChar(100)
  mobile       String?    @db.VarChar(100)
  email        String?    @db.VarChar(100)
  company      String?    @db.VarChar(255)
  address      String?    @db.VarChar(255)
  city         String?    @db.VarChar(100)
  balance      Float?
  demo_balance Float?
  country_id   Int?
  campaign_id  Int?
  countries    countries? @relation(fields: [country_id], references: [id])

  @@index([country_id], map: "clients_country_id_fkey")
}

model codes {
  id              Int            @id @default(autoincrement())
  code            String?        @db.VarChar(50)
  title           String?        @db.VarChar(255)
  subject         Int?
  teacher         Int?
  value           Decimal?       @db.Decimal(10, 0)
  used            Boolean?
  owner           Int?
  issued_date     DateTime?      @db.DateTime(6)
  expiry_date     DateTime?      @db.DateTime(6)
  redeem_date     DateTime?      @db.DateTime(6)
  comment         String?        @db.VarChar(255)
  active          Int            @default(1) @db.TinyInt
  reset           Int?           @default(0) @db.TinyInt
  reset_date      DateTime?      @db.DateTime(0)
  expiry_days     Int?
  created_at      DateTime?      @default(now()) @db.DateTime(0)
  bundle_id       Int?
  wallet_recharge Int?           @default(0) @db.TinyInt
  bundles         bundles?       @relation(fields: [bundle_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_codes_bundle")
  students        students?      @relation(fields: [owner], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_codes_students1")
  subjects        subjects?      @relation(fields: [subject], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_codes_subjects1")
  teachers        teachers?      @relation(fields: [teacher], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_codes_teachers1")
  paymob_codes    paymob_codes[]

  @@index([owner], map: "fk_codes_students1_idx")
  @@index([subject], map: "fk_codes_subjects1_idx")
  @@index([teacher], map: "fk_codes_teachers1_idx")
  @@index([code], map: "codes_index")
  @@index([bundle_id], map: "fk_codes_bundles1_idx")
}

model cohorts {
  id       Int        @id @default(autoincrement())
  titel    String?    @db.VarChar(100)
  students students[]
}

model comments {
  id                       Int                  @id @default(autoincrement())
  comment                  String?              @db.VarChar(2000)
  created_date             DateTime?            @default(now()) @db.DateTime(0)
  active                   Int?                 @default(1) @db.TinyInt
  old_comment              Int?
  post_id                  Int?
  student_id               Int?
  teacher_id               Int?
  image                    String?              @db.VarChar(300)
  record                   String?              @db.VarChar(300)
  posts                    posts?               @relation(fields: [post_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "post_id")
  students                 students?            @relation(fields: [student_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "student_comment")
  teachers                 teachers?            @relation(fields: [teacher_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "teacher_comment")
  comments_likes           comments_likes[]
  comments_relations       comments_relations[] @relation("comments_relations_comments_idTocomments")
  comments_relations_reply comments_relations[] @relation("comments_relations_replayTocomments")

  @@index([post_id], map: "fk_comments_posts1_idx")
  @@index([student_id], map: "fk_comments_students1_idx")
  @@index([teacher_id], map: "fk_comments_teachers1_idx")
}

model comments_relations {
  id             Int       @id @default(autoincrement())
  comments_id    Int?
  reels_id       Int?
  students_id    Int?
  teachers_id    Int?
  replay         Int?
  comments       comments? @relation("comments_relations_comments_idTocomments", fields: [comments_id], references: [id], onDelete: Cascade, map: "fk_comments_relations_comments1")
  comments_reply comments? @relation("comments_relations_replayTocomments", fields: [replay], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_comments_relations_comments2")
  reels          reels?    @relation(fields: [reels_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_comments_relations_reels1")
  students       students? @relation(fields: [students_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_comments_relations_students1")
  teachers       teachers? @relation(fields: [teachers_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "teachers_ids")

  @@index([comments_id], map: "fk_comments_relations_comments1_idx")
  @@index([replay], map: "fk_comments_relations_comments2_idx")
  @@index([reels_id], map: "fk_comments_relations_reels1_idx")
  @@index([students_id], map: "fk_comments_relations_students1_idx")
  @@index([teachers_id], map: "fk_comments_relations_teachers1_idx")
}

model contacts {
  id          Int        @id @default(autoincrement())
  firstname   String?    @db.VarChar(255)
  lastname    String?    @db.VarChar(255)
  phone       String?    @db.VarChar(100)
  mobile      String?    @db.VarChar(100)
  email       String     @db.VarChar(100)
  company     String?    @db.VarChar(255)
  address     String?    @db.VarChar(255)
  city        String?    @db.VarChar(100)
  country_id  Int?
  comments    String?    @db.VarChar(1000)
  salesman_id Int?
  campaign_id Int?
  manager_id  Int?
  created_at  DateTime?  @db.DateTime(6)
  job         String?    @db.VarChar(255)
  last_update DateTime?  @db.DateTime(6)
  status_id   Int?
  school      String?    @db.VarChar(255)
  grade       String?    @db.VarChar(255)
  countries   countries? @relation(fields: [country_id], references: [id])
  status      status?    @relation(fields: [status_id], references: [id])
  events      events[]
  history     history[]

  @@index([country_id], map: "contacts_country_id_fkey")
  @@index([salesman_id], map: "contacts_salesman_id_fkey")
  @@index([status_id], map: "contacts_status_id_fkey")
}

model countries {
  id                 Int                @id @default(autoincrement())
  title              String?            @db.VarChar(255)
  auto_price         Int?
  support            Int?
  gram_price         Float?
  currency           String?            @db.VarChar(100)
  code               String?            @db.VarChar(45)
  phone_code         String?            @db.VarChar(45)
  showed_title       String?            @db.VarChar(45)
  pound_to_cents     Int?               @default(100)
  ashtar_codes       Int?               @default(1) @db.TinyInt
  wallet             Int?               @default(1) @db.TinyInt
  electronic_wallet  Int?               @default(1) @db.TinyInt
  credit             Int?               @default(1) @db.TinyInt
  fawry              Int?               @default(1) @db.TinyInt
  apple_pay_currency String?            @db.VarChar(45)
  branch_countries   branch_countries[]
  cities             cities[]
  clients            clients[]
  contacts           contacts[]
  package_price      package_price[]
  teachers           teachers[]
  wallets            wallets[]
}

model coupons {
  id                          Int                        @id @default(autoincrement())
  title                       String?                    @db.VarChar(255)
  subject                     Int?
  teacher                     Int?
  value                       Decimal?                   @db.Decimal(10, 0)
  used                        Boolean?
  owner                       Int?
  issued_date                 DateTime?                  @db.DateTime(0)
  expiry_date                 DateTime?                  @db.DateTime(0)
  redeem_date                 DateTime?                  @db.DateTime(0)
  activity                    String?                    @db.VarChar(100)
  activity_id                 Int?
  comment                     String?                    @db.VarChar(255)
  section_id                  Int?
  lesson_id                   Int?
  type                        String?                    @db.VarChar(45)
  duration                    Int?
  created_at                  DateTime?                  @default(now()) @db.DateTime(0)
  limit                       Int?
  active                      Int?                       @default(1) @db.TinyInt
  transactions_our_teacher_id Int?
  assignment_id               Int?
  activities                  activities?                @relation(fields: [activity_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_activity_couponse")
  assignments                 assignments?               @relation(fields: [assignment_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_assignment")
  lessons                     lessons?                   @relation(fields: [lesson_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_coupons_lessons1")
  sections                    sections?                  @relation(fields: [section_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_coupons_sections1")
  subjects                    subjects?                  @relation(fields: [subject], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_coupons_subjects1")
  transactions_our_teachers   transactions_our_teachers? @relation(fields: [transactions_our_teacher_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_transaction_couponse")
  students                    students?                  @relation(fields: [owner], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "owner")

  @@index([owner], map: "owner")
  @@index([lesson_id], map: "fk_coupons_lessons1_idx")
  @@index([section_id], map: "fk_coupons_sections1_idx")
  @@index([subject], map: "fk_coupons_subjects1_idx")
  @@index([activity_id], map: "fk_activity_couponse_idx")
  @@index([assignment_id], map: "fk_assignment_idx")
  @@index([title], map: "fk_title_coupons")
  @@index([transactions_our_teacher_id], map: "fk_transaction_couponse_idx")
}

model courses_groups {
  id                          Int                           @id @default(autoincrement())
  name                        String?                       @db.VarChar(45)
  subjects_id                 Int
  week_day                    Int?
  start_date                  DateTime?                     @db.DateTime(0)
  confirmation                Int?                          @default(0) @db.TinyInt
  course_group_submit_app     course_group_submit_app[]
  subjects                    subjects                      @relation(fields: [subjects_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_courses_groups_subjects1")
  sessions                    sessions[]
  students_has_courses_groups students_has_courses_groups[]

  @@index([subjects_id], map: "fk_courses_groups_subjects1_idx")
}

model enrollments {
  id                         Int                        @id @default(autoincrement())
  student                    Int?
  subject                    Int?
  teacher                    Int?
  code                       String?                    @db.VarChar(50)
  enrollment_date            DateTime?                  @db.DateTime(0)
  points                     Int?
  active                     Boolean?                   @default(true)
  balance                    Int?
  group                      Int?
  comments                   String?                    @db.VarChar(500)
  old_student                Int?
  online                     Int?                       @db.TinyInt
  deactivater                String?                    @db.VarChar(45)
  role_deactivater           String?                    @db.VarChar(45)
  term_id                    Int?
  our_teacher_transaction_id Int?
  expiry_date                DateTime?                  @db.DateTime(0)
  archive                    Int?                       @default(0) @db.TinyInt
  withdraw                   Int?                       @default(0) @db.TinyInt
  students                   students?                  @relation(fields: [student], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_enrollments_students1")
  terms                      terms?                     @relation(fields: [term_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_enrollments_term")
  transactions_our_teachers  transactions_our_teachers? @relation(fields: [our_teacher_transaction_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_enrollments_our_teacher_transaction_id")
  subjects                   subjects?                  @relation(fields: [subject], references: [id], onDelete: Cascade, map: "subject")
  teachers                   teachers?                  @relation(fields: [teacher], references: [id], onDelete: Cascade, map: "teacher_id")

  @@index([subject], map: "subject")
  @@index([student], map: "fk_enrollments_students1_idx")
  @@index([term_id], map: "fk_enrollments_terms1_idx")
  @@index([our_teacher_transaction_id], map: "fk_enrollments_transactions_our_teachers1_idx")
  @@index([teacher, subject], map: "idx_teacher_subject")
}

model event_qr_code {
  id            Int       @id @unique(map: "id_UNIQUE") @default(autoincrement())
  qr_code       String    @db.VarChar(45)
  event_id      Int
  student_id    Int
  course_id     Int
  section_id    Int
  attend        Int?      @default(0) @db.TinyInt
  generate_date DateTime? @db.DateTime(6)
  expire_date   DateTime? @db.DateTime(6)
  attend_date   DateTime? @db.DateTime(6)
  group         Int?
  sections      sections  @relation(fields: [section_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_event_qr_code_sections1")

  @@index([section_id], map: "fk_event_qr_code_sections1_idx")
}

model events {
  id          Int       @id @default(autoincrement())
  contact_id  Int?
  salesman_id Int?
  type        String?   @db.VarChar(255)
  title       String?   @db.VarChar(255)
  event_date  String?   @db.VarChar(100)
  event_time  String?   @db.VarChar(100)
  status      String?   @db.VarChar(1000)
  feedback    String?   @db.VarChar(1000)
  schedule    Int?
  created_at  DateTime? @db.DateTime(6)
  contacts    contacts? @relation(fields: [contact_id], references: [id])
  users       users?    @relation(fields: [salesman_id], references: [id])

  @@index([contact_id], map: "events_contact_id_fkey")
  @@index([salesman_id], map: "events_salesman_id_fkey")
}

model exams {
  id                 Int                  @id @default(autoincrement())
  title              String?              @db.VarChar(105)
  grade              Int?
  sessions_id        Int?
  assignment_id      Int?
  open_date          DateTime?            @db.DateTime(0)
  closed_date        DateTime?            @db.DateTime(0)
  assignments        assignments?         @relation(fields: [assignment_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_exams_assignments")
  sessions           sessions?            @relation(fields: [sessions_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_exams_sessions1")
  students_has_exams students_has_exams[]

  @@index([sessions_id], map: "fk_exams_sessions1_idx")
  @@index([assignment_id], map: "idx_exams_assignment")
}

model followers {
  id        Int     @id @default(autoincrement())
  firstname String? @db.VarChar(255)
  lastname  String? @db.VarChar(255)
  gender    String? @db.VarChar(255)
  mobile    String? @db.VarChar(20)
  email     String? @db.VarChar(100)
}

model grades {
  id                        Int                         @id @default(autoincrement())
  title                     String?                     @db.VarChar(255)
  admission                 Boolean?
  branch_id                 Int?
  price                     Int                         @default(50)
  category_id               Int?
  showed_title              String?                     @db.VarChar(45)
  active                    Int?                        @default(1) @db.TinyInt
  old_grades                Int?
  next_step                 Int?
  path                      String?                     @default("home") @db.VarChar(45)
  zaker_seen                Int?                        @default(1) @db.TinyInt
  modrsena_seen             Int?                        @default(1) @db.TinyInt
  questions_bank_seen       Int?                        @default(1) @db.TinyInt
  resources_seen            Int?                        @default(0) @db.TinyInt
  ask_teacher_seen          Int?                        @default(0) @db.TinyInt
  resources_seen_in_home    Int?                        @default(0) @db.TinyInt
  reports_seen              Int?                        @default(0) @db.TinyInt
  calendar_seen             String?                     @default("0") @db.VarChar(255)
  live_session_schedule     String?                     @db.VarChar(400)
  ads_has_grades            ads_has_grades[]
  application               application[]
  ashtar_codes              ashtar_codes[]
  ashtar_enrollments        ashtar_enrollments[]
  calendar_events           calendar_events[]
  classrooms                classrooms[]
  category                  category?                   @relation(fields: [category_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_grades_category1")
  branches                  branches?                   @relation(fields: [branch_id], references: [id], onDelete: Restrict)
  onboarding_steps          onboarding_steps?           @relation(fields: [next_step], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "next_step")
  modarsina_mirror          modarsina_mirror[]
  payments                  payments[]
  reels_has_grades          reels_has_grades[]
  reservation               reservation[]
  student_point_balances    student_point_balances[]
  student_point_earnings    student_point_earnings[]
  student_subscribtions     student_subscribtions[]
  students                  students[]
  subjects                  subjects[]
  subjects_grouping_grades  subjects_grouping_grades[]
  subjects_type             subjects_type[]
  zaker                     zaker[]
  zaker_grade_plans         zaker_grade_plans[]
  zaker_grade_post_handlers zaker_grade_post_handlers[]
  zaker_subscribtion_prices zaker_subscribtion_prices[]

  @@index([category_id], map: "fk_grades_category1_idx")
  @@index([branch_id], map: "grades_branch_id_fkey")
  @@index([next_step], map: "next_step_idx")
}

model groups {
  id      Int     @id @default(autoincrement())
  title   String? @db.VarChar(255)
  type    String? @db.VarChar(255)
  subject Int?
  teacher Int?
}

model history {
  id           Int       @id @default(autoincrement())
  contact_id   Int?
  status_id    Int?
  status_title String?   @db.VarChar(100)
  salesman_id  Int?
  update_date  String?   @db.VarChar(100)
  description  String?   @db.VarChar(255)
  update_time  String?   @db.VarChar(100)
  contacts     contacts? @relation(fields: [contact_id], references: [id])
  users        users?    @relation(fields: [salesman_id], references: [id])
  status       status?   @relation(fields: [status_id], references: [id])

  @@index([contact_id], map: "history_contact_id_fkey")
  @@index([salesman_id], map: "history_salesman_id_fkey")
  @@index([status_id], map: "history_status_id_fkey")
}

model lessons {
  id                     Int                    @id @default(autoincrement())
  title                  String?                @db.VarChar(255)
  subject                Int?
  order                  Int?
  hide                   Boolean?
  type                   String?                @db.VarChar(100)
  date                   DateTime?              @db.DateTime(6)
  breakdown              String?                @db.VarChar(100)
  teacher                Int?
  live                   Boolean?
  live_link              String?                @db.VarChar(255)
  price_type             String?                @db.VarChar(255)
  price                  Int?
  fawry                  Int                    @default(0)
  fawry_price            Int?
  section                Int?
  lessons_sequence       Int?
  deleted                Boolean?               @default(false)
  zoom                   Int?
  term_id                Int?
  is_online              Int?                   @default(0) @db.TinyInt
  online_price           Int?
  online_enroll_duration Int?                   @default(0)
  vimeo_id               String?                @db.VarChar(255)
  event_date             DateTime?              @db.Date
  activities             activities[]
  advertise_details      advertise_details[]
  assignments            assignments[]
  attendance             attendance[]
  calendar_events        calendar_events[]
  coupons                coupons[]
  sections               sections?              @relation(fields: [section], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_lessons_sections1")
  subjects               subjects?              @relation(fields: [subject], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_lessons_subjects1")
  zoom_meeting           zoom_meeting?          @relation(fields: [zoom], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_lessons_zoom_meeting1")
  terms                  terms?                 @relation(fields: [term_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_lessons_term_id")
  live_rooms             live_rooms[]
  meeting_details        meeting_details[]
  point_configurations   point_configurations[]
  post_tags              post_tags[]
  questions              questions[]
  sessions               sessions[]

  @@index([section], map: "fk_lessons_sections1_idx")
  @@index([subject], map: "fk_lessons_subjects1_idx")
  @@index([zoom], map: "fk_lessons_zoom_meeting1_idx")
  @@index([term_id], map: "fk_terms_lessons_idx")
}

model lessons_details {
  lesson_id   Int     @id
  objectives  String? @db.VarChar(255)
  weekly_plan String? @db.VarChar(255)
}

model likes {
  students_id  Int
  reels_id     Int
  created_date DateTime? @db.DateTime(0)
  reels        reels     @relation(fields: [reels_id], references: [id], onDelete: Cascade, map: "fk_students_has_reels_reels1")
  students     students  @relation(fields: [students_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_students_has_reels_students1")

  @@id([students_id, reels_id])
  @@index([reels_id], map: "fk_students_has_reels_reels1_idx")
  @@index([students_id], map: "fk_students_has_reels_students1_idx")
}

model live_questions {
  id            Int       @id @default(autoincrement())
  lesson        Int?
  student       Int?
  question      String?   @db.VarChar(2000)
  question_link String?   @db.VarChar(255)
  question_time DateTime? @db.DateTime(6)
  students      students? @relation(fields: [student], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "students")

  @@index([student], map: "students")
}

model lives {
  id          Int           @id @default(autoincrement())
  title       String?       @db.VarChar(255)
  link        String        @db.VarChar(255)
  live_date   DateTime?     @db.DateTime(0)
  live_time   String?       @db.VarChar(255)
  subject     Int
  lesson      Int?
  description String?       @db.VarChar(255)
  stream_key  String        @db.VarChar(70)
  status      lives_status? @default(created)
  created_at  DateTime?     @default(now()) @db.DateTime(0)
  uri         String?       @default("/live_events/3609728") @db.VarChar(45)
  subjects    subjects      @relation(fields: [subject], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_lives_subjects1")

  @@index([subject], map: "fk_lives_subjects1_idx")
}

model notifications {
  id                         Int                          @id @default(autoincrement())
  title                      String                       @db.VarChar(100)
  message                    String                       @db.VarChar(1000)
  datetime                   DateTime?                    @default(now()) @db.DateTime(0)
  link                       String?                      @db.VarChar(200)
  notifications_has_parents  notifications_has_parents[]
  notifications_has_students notifications_has_students[]
}

model notifications_has_parents {
  notifications_id Int
  parents_id       Int
  notifications    notifications @relation(fields: [notifications_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_notifications_has_parents_notifications1")
  parent           parent        @relation(fields: [parents_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_notifications_has_parents_parent1")

  @@id([notifications_id, parents_id])
  @@index([parents_id], map: "fk_notifications_has_parents_parent1_idx")
}

model notifications_has_students {
  notifications_id Int
  students_id      Int
  notifications    notifications @relation(fields: [notifications_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_notifications_has_students_notifications1")
  students         students      @relation(fields: [students_id], references: [id], onDelete: Cascade, map: "fk_notifications_has_students_students1")

  @@id([notifications_id, students_id])
  @@index([notifications_id], map: "fk_notifications_has_students_notifications1_idx")
  @@index([students_id], map: "fk_notifications_has_students_students1_idx")
}

model otp_verify_codes {
  id         Int       @id @unique(map: "id_UNIQUE") @default(autoincrement())
  Phone      String?   @db.VarChar(45)
  Code       String?   @db.VarChar(45)
  ExpireDate DateTime? @db.Date
  RquestedAt DateTime? @db.Date
  IsVerified Int?      @db.TinyInt
}

model parent {
  id                        Int                         @id @default(autoincrement())
  firstname                 String?                     @db.VarChar(45)
  lastname                  String?                     @db.VarChar(45)
  phone                     String?                     @db.VarChar(45)
  email                     String?                     @db.VarChar(45)
  uid                       String?                     @db.VarChar(45)
  is_online                 Int?                        @default(0) @db.TinyInt
  parent                    Int?                        @default(1) @db.TinyInt
  password                  String?                     @db.VarChar(255)
  notifications_has_parents notifications_has_parents[]
  parent_devices            parent_devices[]
  parent_has_students       parent_has_students[]
  wallets                   wallets[]
}

model parent_devices {
  id           Int     @id @default(autoincrement())
  device_token String? @db.VarChar(225)
  parent_id    Int?
  mac_address  String? @db.VarChar(255)
  parent       parent? @relation(fields: [parent_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_parent_devices_parent1")

  @@index([parent_id], map: "fk_parent_devices_parent1_idx")
}

model parent_has_students {
  parent_id   Int
  students_id Int
  parent      parent   @relation(fields: [parent_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_parent_has_students_parent1")
  students    students @relation(fields: [students_id], references: [id], onDelete: Cascade, map: "fk_parent_has_students_students1")

  @@id([parent_id, students_id])
  @@index([parent_id], map: "fk_parent_has_students_parent1_idx")
  @@index([students_id], map: "fk_parent_has_students_students1_idx")
}

model payments {
  id                            Int                         @id @default(autoincrement())
  student                       Int?
  grade_id                      Int?
  amount                        Decimal?                    @db.Decimal(10, 2)
  payment_date                  DateTime?                   @db.DateTime(6)
  status                        payments_status?
  ref                           String                      @unique(map: "ref_UNIQUE") @db.VarChar(150)
  description                   String?                     @db.VarChar(255)
  InvoiceId                     String?                     @db.VarChar(255)
  payment_method                String?                     @default("fawry") @db.VarChar(45)
  terms_id                      Int?
  zaker_subscribtions_prices_id Int?
  is_wallet                     Boolean?                    @default(false)
  partial_wallet_amount         Decimal?                    @default(0.00) @db.Decimal(10, 2)
  payment_paid_for              payments_payment_paid_for?  @default(subject)
  originalPrice                 Decimal?                    @db.Decimal(10, 2)
  question_bank                 Int?                        @default(0) @db.TinyInt
  currency                      String?                     @default("EGP") @db.VarChar(45)
  promo_code_id                 Int?
  uuid                          Bytes?                      @unique(map: "uuid_UNIQUE") @db.Binary(16)
  grade_plan_id                 Int?
  live_session                  Int?                        @default(0) @db.TinyInt
  domain                        String?                     @db.VarChar(500)
  FawryPayment                  FawryPayment[]
  payment_details               payment_details[]
  zaker_grade_plans             zaker_grade_plans?          @relation(fields: [grade_plan_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_payments_grade_plan_id")
  grades                        grades?                     @relation(fields: [grade_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: " fk_payment_grades_id")
  promo_codes                   promo_codes?                @relation(fields: [promo_code_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_payments_promo_code_id")
  students                      students?                   @relation(fields: [student], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_payments_students1")
  terms                         terms?                      @relation(fields: [terms_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_payments_terms_id")
  zaker_subscribtion_prices     zaker_subscribtion_prices?  @relation(fields: [zaker_subscribtions_prices_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_paymet_zaker_subscribtions_prices_id")
  transactions_our_teachers     transactions_our_teachers[]

  @@index([payment_method], map: "idex_payment_type")
  @@index([grade_id], map: "fk_payments_grades1_idx")
  @@index([promo_code_id], map: "fk_payments_promo_code_idx")
  @@index([terms_id], map: "fk_payments_terms1_idx")
  @@index([zaker_subscribtions_prices_id], map: "fk_payments_zaker_subscribtion_prices1_idx")
  @@index([student], map: "students_payments")
  @@index([grade_plan_id], map: "fk_payments_grade_plan_id_idx")
}

model permissions {
  id                         Int                          @id @default(autoincrement())
  title                      String?                      @unique(map: "title_UNIQUE") @db.VarChar(75)
  route                      String?                      @db.VarChar(75)
  guard_name                 String?                      @default("api") @db.VarChar(45)
  route_name                 String?                      @db.VarChar(45)
  url                        String?                      @db.VarChar(45)
  permissions_has_students   permissions_has_students[]
  permissions_has_super_user permissions_has_super_user[]
  permissions_has_users      permissions_has_users[]
  roles_has_permissions      roles_has_permissions[]
}

model permissions_has_students {
  permissions_id Int
  students_id    Int
  permissions    permissions @relation(fields: [permissions_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_permissions_has_students_permissions1")
  students       students    @relation(fields: [students_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_permissions_has_students_students1")

  @@id([permissions_id, students_id])
  @@index([permissions_id], map: "fk_permissions_has_students_permissions1_idx")
  @@index([students_id], map: "fk_permissions_has_students_students1_idx")
}

model permissions_has_super_user {
  permissions_id Int
  super_user_id  Int
  permissions    permissions @relation(fields: [permissions_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_permissions_has_super_user_permissions1")
  super_user     super_user  @relation(fields: [super_user_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_permissions_has_super_user_super_user1")

  @@id([permissions_id, super_user_id])
  @@index([permissions_id], map: "fk_permissions_has_super_user_permissions1_idx")
  @@index([super_user_id], map: "fk_permissions_has_super_user_super_user1_idx")
}

model permissions_has_users {
  permissions_id Int
  users_id       Int
  permissions    permissions @relation(fields: [permissions_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_permissions_has_users_permissions1")
  users          users       @relation(fields: [users_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_permissions_has_users_users1")

  @@id([permissions_id, users_id])
  @@index([permissions_id], map: "fk_permissions_has_users_permissions1_idx")
  @@index([users_id], map: "fk_permissions_has_users_users1_idx")
}

model questions {
  id                        Int                         @id @default(autoincrement())
  lesson                    Int?
  title                     String?                     @db.VarChar(4000)
  type                      String?                     @db.VarChar(255)
  difficulty                Int?
  dok                       Int?
  bloom                     Int?
  answer                    String?                     @db.VarChar(1500)
  feedback                  String?                     @db.VarChar(1000)
  standard                  Int?
  subject                   Int?
  owner                     Int?
  tags                      String?                     @db.VarChar(255)
  duration                  Int?
  image                     String?                     @db.VarChar(2000)
  video                     String?                     @db.VarChar(1000)
  sound                     String?                     @db.VarChar(255)
  link                      String?                     @db.VarChar(255)
  answer_feedback           String?                     @db.VarChar(2000)
  answer_image              String?                     @db.VarChar(1000)
  answer_video              String?                     @db.VarChar(1000)
  answer_sound              String?                     @db.VarChar(255)
  answer_link               String?                     @db.VarChar(255)
  review                    String?                     @db.VarChar(255)
  reviewer                  Int?
  review_time               String?                     @db.VarChar(100)
  last_edit                 String?                     @db.VarChar(100)
  editor                    Int?
  math_equation             String?                     @db.VarChar(45)
  createdAt                 DateTime?                   @default(now()) @db.Timestamp(0)
  in_question_bank          Int?                        @default(0) @db.TinyInt
  assignments_has_questions assignments_has_questions[]
  lessons                   lessons?                    @relation(fields: [lesson], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_questions_lessons1")
  subjects                  subjects?                   @relation(fields: [subject], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_questions_subjects1")
  questions_options         questions_options[]
  submission_details        submission_details[]

  @@index([lesson], map: "fk_questions_lessons1_idx")
  @@index([subject], map: "fk_questions_subjects1_idx")
}

model questions_options {
  id                 Int                  @id @default(autoincrement())
  question           Int?
  title              String?              @db.VarChar(255)
  sound              String?              @db.VarChar(255)
  correct            Boolean?
  questions          questions?           @relation(fields: [question], references: [id], onDelete: Cascade, map: "options")
  submission_details submission_details[]

  @@index([question], map: "options")
}

model reels {
  id                 Int                  @id @unique(map: "id_UNIQUE") @default(autoincrement())
  teacher            Int?
  title              String?              @db.VarChar(45)
  image              String?              @db.VarChar(1000)
  video              String?              @db.VarChar(1000)
  issued_date        DateTime?            @default(now()) @db.DateTime(0)
  expiry_date        DateTime?            @db.DateTime(0)
  active             Boolean?             @default(true)
  old_reels          Int?                 @db.TinyInt
  views              Int?                 @default(0)
  shares             Int?                 @default(0)
  watch_time         Int?                 @default(0)
  start_date         DateTime?            @db.DateTime(0)
  comments_relations comments_relations[]
  likes              likes[]
  teachers           teachers?            @relation(fields: [teacher], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_reels_teachers1")
  reels_has_grades   reels_has_grades[]

  @@index([teacher], map: "fk_reels_teachers1_idx")
}

model reels_has_grades {
  reels_id  Int
  grades_id Int
  grades    grades @relation(fields: [grades_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_reels_has_grades_grades1")
  reels     reels  @relation(fields: [reels_id], references: [id], onDelete: Cascade, map: "fk_reels_has_grades_reels1")

  @@id([reels_id, grades_id])
  @@index([grades_id], map: "fk_reels_has_grades_grades1_idx")
  @@index([reels_id], map: "fk_reels_has_grades_reels1_idx")
}

model resources {
  id                        Int                      @id @default(autoincrement())
  title                     String                   @db.VarChar(255)
  type                      String?                  @db.VarChar(100)
  lesson                    Int?
  link                      String?                  @db.VarChar(255)
  order                     Int?
  description               String?                  @db.VarChar(255)
  cover_url                 String?                  @db.VarChar(500)
  teacher_id                Int
  term_id                   Int?
  subject_grouping_grade_id Int
  activity_id               Int?
  assignment_id             Int?
  activities                activities?              @relation(fields: [activity_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_activity_id_rescources")
  assignments               assignments?             @relation(fields: [assignment_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_assignmentId_resources")
  teachers                  teachers                 @relation(fields: [teacher_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_resources_teachers1")
  subjects_grouping_grades  subjects_grouping_grades @relation(fields: [subject_grouping_grade_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_subject_grouping_resources")
  terms                     terms?                   @relation(fields: [term_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_termId_resources")

  @@index([activity_id], map: "fk_activity_id_rescources_idx")
  @@index([assignment_id], map: "fk_assignmentId_resources_idx")
  @@index([teacher_id], map: "fk_resources_teachers1_idx")
  @@index([subject_grouping_grade_id], map: "fk_subject_grouping_resources_idx")
  @@index([term_id], map: "fk_termId_resources_idx")
}

model roles {
  id                    Int                     @id @default(autoincrement())
  title                 String                  @unique(map: "title_UNIQUE") @db.VarChar(75)
  roles_has_permissions roles_has_permissions[]
  roles_has_students    roles_has_students[]
  roles_has_super_user  roles_has_super_user[]
  roles_has_users       roles_has_users[]
}

model roles_has_permissions {
  roles_id       Int
  permissions_id Int
  permissions    permissions @relation(fields: [permissions_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_roles_has_permissions_permissions1")
  roles          roles       @relation(fields: [roles_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_roles_has_permissions_roles1")

  @@id([roles_id, permissions_id])
  @@index([permissions_id], map: "fk_roles_has_permissions_permissions1_idx")
  @@index([roles_id], map: "fk_roles_has_permissions_roles1_idx")
}

model roles_has_students {
  roles_id    Int
  students_id Int
  roles       roles    @relation(fields: [roles_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_roles_has_students_roles1")
  students    students @relation(fields: [students_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_roles_has_students_students1")

  @@id([roles_id, students_id])
  @@index([roles_id], map: "fk_roles_has_students_roles1_idx")
  @@index([students_id], map: "fk_roles_has_students_students1_idx")
}

model roles_has_super_user {
  roles_id      Int
  super_user_id Int
  roles         roles      @relation(fields: [roles_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_roles_has_super_user_roles1")
  super_user    super_user @relation(fields: [super_user_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_roles_has_super_user_super_user1")

  @@id([roles_id, super_user_id])
  @@index([roles_id], map: "fk_roles_has_super_user_roles1_idx")
  @@index([super_user_id], map: "fk_roles_has_super_user_super_user1_idx")
}

model roles_has_users {
  roles_id Int
  users_id Int
  roles    roles @relation(fields: [roles_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_roles_has_users_roles1")
  users    users @relation(fields: [users_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_roles_has_users_users1")

  @@id([roles_id, users_id])
  @@index([roles_id], map: "fk_roles_has_users_roles1_idx")
  @@index([users_id], map: "fk_roles_has_users_users1_idx")
}

model rooms {
  id          Int       @id @default(autoincrement())
  calendar_id String    @db.VarChar(90)
  name        String?   @db.VarChar(45)
  calendars   calendars @relation(fields: [calendar_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_rooms_calendars1")

  @@index([calendar_id], map: "fk_rooms_calendars1_idx")
}

model schedules {
  id           Int     @id @default(autoincrement())
  group        Int?
  attend_day   String? @db.VarChar(100)
  attend_time  String? @db.VarChar(100)
  leaving_time String? @db.VarChar(100)
}

model schools {
  id     Int      @id @default(autoincrement())
  title  String?  @db.VarChar(255)
  active Boolean?
  logo   String?  @db.VarChar(255)
}

model sections {
  id                     Int                     @id @default(autoincrement())
  teacher                Int?
  subject                Int?
  title                  String?                 @db.VarChar(255)
  order                  Int?
  price                  Int?                    @default(0)
  fawry                  Int?                    @default(0)
  fawry_price            Int?                    @default(0)
  active                 Int?                    @default(1) @db.TinyInt
  tag                    String?                 @db.VarChar(45)
  online                 Int?                    @db.TinyInt
  is_online              Int?                    @default(0) @db.TinyInt
  online_price           Int?
  advertise_description  String?                 @db.VarChar(255)
  online_enroll_duration Int?                    @default(0)
  advertise_price        Int?
  end_date               DateTime?               @db.DateTime(0)
  start_date             DateTime?               @db.DateTime(0)
  live_session           Int?                    @default(0) @db.TinyInt
  coupons                coupons[]
  event_qr_code          event_qr_code[]
  lessons                lessons[]
  point_configurations   point_configurations[]
  subjects               subjects?               @relation(fields: [subject], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_sections_subjects1")
  sessions               sessions[]
  students_has_sections  students_has_sections[]

  @@index([subject], map: "fk_sections_subjects1_idx")
}

model securitty_roles {
  id            Int      @id @default(autoincrement())
  group         Int?
  role          String?  @db.VarChar(100)
  description   String?  @db.VarChar(255)
  module        String?  @db.VarChar(255)
  list_records  Boolean?
  add_record    Boolean?
  update_record Boolean?
  delete_record Boolean?
}

model security_groups {
  id          Int     @id @default(autoincrement())
  group       String? @db.VarChar(100)
  description String? @db.VarChar(255)
}

model sequences_lessons {
  id             Int          @id @default(autoincrement())
  activities_id  Int?
  assignments_id Int?
  order          Int?
  activities     activities?  @relation(fields: [activities_id], references: [id], onDelete: Cascade, map: "fk_sequences_lessons_activities1")
  assignments    assignments? @relation(fields: [assignments_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_sequences_lessons_assignments1")

  @@index([activities_id], map: "fk_sequences_lessons_activities1_idx")
  @@index([assignments_id], map: "fk_sequences_lessons_assignments1_idx")
}

model sessions {
  id                Int             @id @default(autoincrement())
  title             String?         @db.VarChar(255)
  session_date      DateTime?       @db.DateTime(0)
  description       String?         @db.VarChar(255)
  teachers_id       Int?
  subjects_id       Int?
  sections_id       Int?
  lessons_id        Int?
  courses_groups_id Int?
  video_activity_id Int?
  order             Int?
  sms_sent          Int?            @default(0) @db.TinyInt
  created_at        DateTime?       @default(now()) @db.DateTime(0)
  updated_at        DateTime?       @default(now()) @db.DateTime(0)
  attendance        attendance[]
  exams             exams[]
  courses_groups    courses_groups? @relation(fields: [courses_groups_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_sessions_courses_groups")
  lessons           lessons?        @relation(fields: [lessons_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_sessions_lessons1")
  sections          sections?       @relation(fields: [sections_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_sessions_sections1")
  subjects          subjects?       @relation(fields: [subjects_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_sessions_subjects1")
  teachers          teachers?       @relation(fields: [teachers_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_sessions_teachers1")
  activities        activities?     @relation(fields: [video_activity_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_sessions_video_activity")
  sms_logs          sms_logs[]

  @@index([subjects_id], map: "fk_sessions_subjects1_idx")
  @@index([teachers_id], map: "fk_sessions_teachers1_idx")
  @@index([lessons_id], map: "fk_sessions_lessons1_idx")
  @@index([sections_id], map: "fk_sessions_sections1_idx")
  @@index([video_activity_id], map: "fk_sessions_video_activity")
  @@index([courses_groups_id], map: "idx_sessions_courses_groups")
}

model settings {
  id       Int     @id @default(autoincrement())
  key      String  @unique(map: "key_UNIQUE") @db.VarChar(90)
  value    String? @db.VarChar(45)
  returned Int?    @default(1) @db.TinyInt
}

model signals {
  id       Int     @id @default(autoincrement())
  activity Int?
  intime   Int?
  totime   Int?
  type     String? @db.VarChar(255)
  link     String? @db.VarChar(255)
  question Int?
}

model standards {
  id          Int     @id @default(autoincrement())
  code        String? @db.VarChar(255)
  domain      String? @db.VarChar(255)
  cluster     String? @db.VarChar(255)
  title       String? @db.VarChar(1000)
  parent      Int?
  subject     Int?
  description String? @db.VarChar(1000)
}

model status {
  id       Int        @id @default(autoincrement())
  title    String?    @db.VarChar(255)
  type     String?    @db.VarChar(255)
  color    String?    @db.VarChar(100)
  icon     String?    @db.VarChar(100)
  contacts contacts[]
  history  history[]
}

model student_devices {
  id           Int       @id @default(autoincrement())
  student_id   Int?
  mac_address  String?   @db.VarChar(45)
  device_type  String?   @db.VarChar(45)
  active       Int?      @db.TinyInt
  device_token String?   @db.VarChar(225)
  students     students? @relation(fields: [student_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_student_devices_students1")

  @@index([student_id], map: "fk_student_devices_students1_idx")
}

model students {
  id                              Int                               @id @default(autoincrement())
  uid                             String?                           @db.VarChar(255)
  firstname                       String?                           @db.VarChar(255)
  lastname                        String?                           @db.VarChar(255)
  gender                          String?                           @db.VarChar(255)
  secondlanguage                  String?                           @db.VarChar(255)
  religion                        String?                           @db.VarChar(255)
  phone                           String?                           @db.VarChar(100)
  mobile                          String?                           @db.VarChar(100)
  email                           String?                           @db.VarChar(255)
  system                          String?                           @db.VarChar(255)
  is_online                       Int?                              @default(0) @db.TinyInt
  branch_id                       Int?
  grade_id                        Int?
  grade                           String?                           @db.VarChar(255)
  citizenship                     String?                           @db.VarChar(255)
  withdraw                        Boolean?
  points                          Int?
  date_of_birth                   String?                           @db.VarChar(100)
  image                           String?                           @db.VarChar(255)
  address                         String?                           @db.VarChar(255)
  preschool                       String?                           @db.VarChar(255)
  father_mobile                   String?                           @db.VarChar(255)
  father_email                    String?                           @db.VarChar(255)
  father_degree                   String?                           @db.VarChar(255)
  father_company                  String?                           @db.VarChar(255)
  father_occupation               String?                           @db.VarChar(255)
  mother_name                     String?                           @db.VarChar(255)
  mother_mobile                   String?                           @db.VarChar(100)
  mother_email                    String?                           @db.VarChar(255)
  mother_degree                   String?                           @db.VarChar(255)
  mother_company                  String?                           @db.VarChar(255)
  mother_occupation               String?                           @db.VarChar(255)
  previous_school                 String?                           @db.VarChar(255)
  havesibling                     String?                           @db.VarChar(50)
  parentsEmployee                 String?                           @db.VarChar(50)
  username                        String?                           @db.VarChar(255)
  password                        String?                           @db.VarChar(255)
  national_id                     String?                           @db.VarChar(100)
  father_nationalid               String?                           @db.VarChar(100)
  mother_nationalid               String?                           @db.VarChar(100)
  classroom_id                    Int?
  cohort_id                       Int?
  lang                            String?                           @db.VarChar(20)
  country                         String?                           @db.VarChar(20)
  system_title                    String?                           @db.VarChar(255)
  grade_title                     String?                           @db.VarChar(255)
  token                           String?                           @db.VarChar(900)
  slug                            String?                           @unique(map: "slug_UNIQUE") @db.VarChar(500)
  student_specific_information    Json?
  created_at                      DateTime?                         @default(now()) @db.DateTime(0)
  city_id                         Int?                              @default(1)
  isPending                       Int?                              @default(0) @db.TinyInt
  web_token                       String?                           @db.VarChar(900)
  is_web                          Int?                              @default(0) @db.TinyInt
  admin_reviewer                  students_admin_reviewer?          @default(not_attempted)
  activities_comments             activities_comments[]
  activities_has_students         activities_has_students[]
  activities_likes                activities_likes[]
  activities_progress             activities_progress[]
  ashtar_codes                    ashtar_codes[]
  ashtar_enrollments              ashtar_enrollments[]
  attendance                      attendance[]
  buyers                          buyers[]
  codes                           codes[]
  comments                        comments[]
  comments_likes                  comments_likes[]
  comments_relations              comments_relations[]
  coupons                         coupons[]
  course_group_submit_app         course_group_submit_app[]
  enrollments                     enrollments[]
  likes                           likes[]
  live_questions                  live_questions[]
  notifications_has_students      notifications_has_students[]
  offer_views                     offer_views[]
  old_ashtar_enrollments          old_ashtar_enrollments[]
  parent_has_students             parent_has_students[]
  payments                        payments[]
  permissions_has_students        permissions_has_students[]
  post_likes                      post_likes[]
  posts                           posts[]
  private_subjects_students       private_subjects_students[]
  promo_codes_as_owner            promo_codes[]                     @relation("promo_code_owner")
  promo_codes_as_receiver         promo_codes[]                     @relation("promo_code_receiver")
  reservation                     reservation[]
  reviews                         reviews[]
  roles_has_students              roles_has_students[]
  sms_logs                        sms_logs[]
  student_calendar                student_calendar[]
  student_devices                 student_devices[]
  student_point_balances          student_point_balances[]
  student_point_earnings          student_point_earnings[]
  student_post_seen               student_post_seen[]
  student_subscribtions           student_subscribtions[]
  cities                          cities?                           @relation(fields: [city_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "students_has_city")
  branches                        branches?                         @relation(fields: [branch_id], references: [id], onDelete: Restrict)
  classrooms                      classrooms?                       @relation(fields: [classroom_id], references: [id], onDelete: Restrict)
  cohorts                         cohorts?                          @relation(fields: [cohort_id], references: [id], onDelete: Restrict)
  grades                          grades?                           @relation(fields: [grade_id], references: [id], onDelete: Restrict)
  students_has_courses_groups     students_has_courses_groups[]
  students_has_exams              students_has_exams[]
  students_has_sections           students_has_sections[]
  students_version                students_version[]
  submission_details              submission_details[]
  submissions                     submissions[]
  transaction_our_teacher_details transaction_our_teacher_details[]
  transactions_advertise_subjects transactions_advertise_subjects[]
  transactions_our_teachers       transactions_our_teachers[]
  user_survey                     user_survey[]
  vendor_offer_codes              vendor_offer_codes[]
  wallets                         wallets[]
  zaker_transactions              zaker_transactions[]

  @@index([branch_id], map: "students_branch_id_fkey")
  @@index([classroom_id], map: "students_classroom_id_fkey")
  @@index([cohort_id], map: "students_cohort_id_fkey")
  @@index([grade_id], map: "students_grade_id_fkey")
  @@index([city_id], map: "city_student_idx")
}

model students_has_courses_groups {
  students_id       Int
  courses_groups_id Int
  card_id           String?        @db.VarChar(50)
  courses_groups    courses_groups @relation(fields: [courses_groups_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_students_has_courses_groups_courses_groups1")
  students          students       @relation(fields: [students_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_students_has_courses_groups_students1")

  @@id([students_id, courses_groups_id])
  @@unique([courses_groups_id, card_id], map: "unique_card_id_per_group")
  @@index([courses_groups_id], map: "fk_students_has_courses_groups_courses_groups1_idx")
  @@index([students_id], map: "fk_students_has_courses_groups_students1_idx")
}

model students_has_exams {
  students_id Int
  exams_id    Int
  grade       Int?     @default(0)
  exams       exams    @relation(fields: [exams_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_students_has_exams_exams1")
  students    students @relation(fields: [students_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_students_has_exams_students1")

  @@id([students_id, exams_id])
  @@index([exams_id], map: "fk_students_has_exams_exams1_idx")
  @@index([students_id], map: "fk_students_has_exams_students1_idx")
}

model students_has_sections {
  students_id Int
  sections_id Int
  score       Int?
  sections    sections @relation(fields: [sections_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_students_has_sections_sections1")
  students    students @relation(fields: [students_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_students_has_sections_students1")

  @@id([students_id, sections_id])
  @@index([sections_id], map: "fk_students_has_sections_sections1_idx")
  @@index([students_id], map: "fk_students_has_sections_students1_idx")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by the Prisma Client.
model students_subjects {
  student Int?
  subject Int?

  @@ignore
}

model subjects {
  id                          Int                         @id @default(autoincrement())
  title                       String?                     @db.VarChar(255)
  grade                       Int?
  mirror                      Int?
  source                      Int?
  participation               Int?
  points                      Int?
  question                    Int?
  videos                      Int?
  sheets                      Int?
  paid                        Boolean?
  active                      Int?                        @default(1)
  icon                        String?                     @db.VarChar(255)
  teacher                     Int?
  price                       Int                         @default(0)
  fawry                       Int                         @default(0)
  fawry_price                 Int?
  branches_id                 Int?
  subjects_type_id            Int?
  sub_system                  String?                     @db.VarChar(45)
  language                    String?                     @db.VarChar(45)
  is_free                     Boolean?
  image                       String?                     @db.VarChar(255)
  web                         Int?                        @default(0) @db.TinyInt
  created_at                  DateTime?                   @default(now()) @db.DateTime(0)
  is_online                   Int                         @default(0) @db.TinyInt
  price_online                Int?
  description                 String?                     @db.VarChar(300)
  ads_description             String?                     @db.VarChar(500)
  percentage_of_attendance    Int?                        @default(50)
  subjects_grouping_grades_id Int?
  avaliable_term              Int?                        @default(0) @db.TinyInt
  enable_question_bank        Int?                        @default(0) @db.TinyInt
  package_id                  Int?
  online_enroll_duration      Int?                        @default(0)
  category                    subjects_category?          @default(general)
  password                    String?                     @db.VarChar(200)
  is_enrollment_locked        Int?                        @default(0) @db.TinyInt
  live_session_image          String?                     @db.VarChar(300)
  activities                  activities[]
  ads                         ads[]
  advertise_subjects          advertise_subjects[]
  ashtar_enrollments          ashtar_enrollments[]
  assignments                 assignments[]
  buyers                      buyers[]
  codes                       codes[]
  coupons                     coupons[]
  courses_groups              courses_groups[]
  enrollments                 enrollments[]
  lessons                     lessons[]
  lives                       lives[]
  modarsina_mirror            modarsina_mirror[]
  payment_details             payment_details[]
  posts                       posts[]
  private_subjects_students   private_subjects_students[]
  promo_codes                 promo_codes[]
  questions                   questions[]
  sections                    sections[]
  sessions                    sessions[]
  subject_bundle              subject_bundle[]
  apple_package               apple_package?              @relation(fields: [package_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_packageId_apple")
  branches                    branches?                   @relation(fields: [branches_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_subjects_branches1")
  grades                      grades?                     @relation(fields: [grade], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_subjects_grades1")
  subjects_grouping_grades    subjects_grouping_grades?   @relation(fields: [subjects_grouping_grades_id], references: [id], onUpdate: NoAction, map: "subject_grouping")
  subjects_type               subjects_type?              @relation(fields: [subjects_type_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_subjects_subjects_type1")
  teachers                    teachers?                   @relation(fields: [teacher], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "teacher")
  subjects_terms              subjects_terms[]
  teacher_versions            teacher_versions[]
  transactions_subjects       transactions_subjects[]
  zaker                       zaker[]
  zaker_grade_post_handlers   zaker_grade_post_handlers[]

  @@index([branches_id], map: "fk_subjects_branches1_idx")
  @@index([grade], map: "fk_subjects_grades1_idx")
  @@index([subjects_type_id], map: "fk_subjects_subjects_type1_idx")
  @@index([teacher], map: "teacher")
  @@index([package_id], map: "fk_packageId_apple_idx")
  @@index([subjects_grouping_grades_id], map: "fk_subjects_subjects_grouping_grades1_idx")
}

model subjects_type {
  id        Int        @id @unique(map: "id_UNIQUE") @default(autoincrement())
  title     String?    @db.VarChar(45)
  icon      String?    @db.VarChar(45)
  grades_id Int?
  subjects  subjects[]
  grades    grades?    @relation(fields: [grades_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_subjects_type_grades1")

  @@index([grades_id], map: "fk_subjects_type_grades1_idx")
}

model submission_attachments {
  id          Int          @id @default(autoincrement())
  submission  Int?
  type        String?      @db.VarChar(100)
  link        String?      @db.VarChar(255)
  upload_date DateTime?    @db.DateTime(0)
  description String?      @db.VarChar(255)
  submissions submissions? @relation(fields: [submission], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "submission")

  @@index([submission], map: "submission")
}

model submissions {
  id                     Int                      @id @default(autoincrement())
  student_id             Int?
  assignment_id          Int?
  grade                  Int?
  right_answers          Int?
  wrong_answers          Int?
  missed_answers         Int?
  submission_date        DateTime?                @db.DateTime(6)
  mark_date              DateTime?                @db.DateTime(6)
  feedback               String?                  @db.VarChar(255)
  description            String?                  @db.VarChar(255)
  updated_at             DateTime?                @default(now()) @db.DateTime(0)
  reviewed               Int?                     @default(1) @db.TinyInt
  submission_attachments submission_attachments[]
  submission_details     submission_details[]
  assignments            assignments?             @relation(fields: [assignment_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "assignment")
  students               students?                @relation(fields: [student_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "student")

  @@index([assignment_id], map: "assignment_id")
  @@index([student_id], map: "student")
}

model super_user {
  id                         Int                          @id @unique(map: "id_UNIQUE") @default(autoincrement())
  firstname                  String?                      @db.VarChar(45)
  lastname                   String?                      @db.VarChar(45)
  email                      String                       @unique(map: "email_UNIQUE") @db.VarChar(45)
  phone                      String?                      @db.VarChar(45)
  NationalId                 String?                      @db.VarChar(45)
  password                   String?                      @db.VarChar(255)
  createdBy                  Int?
  createdAt                  DateTime?                    @default(now()) @db.DateTime(0)
  role                       String?                      @db.VarChar(45)
  type                       Int?                         @default(0) @db.TinyInt
  position                   String?                      @db.VarChar(100)
  ashtar_codes               ashtar_codes[]
  permissions_has_super_user permissions_has_super_user[]
  roles_has_super_user       roles_has_super_user[]
  transactions_our_teachers  transactions_our_teachers[]
  wallets                    wallets[]
  zaker_transactions         zaker_transactions[]
}

model teachers {
  id                              Int                               @id @default(autoincrement())
  firstname                       String?                           @db.VarChar(100)
  lastname                        String?                           @db.VarChar(100)
  title                           String?                           @db.VarChar(50)
  image                           String?                           @db.VarChar(255)
  bio                             String?                           @db.VarChar(1000)
  subject                         String?                           @db.VarChar(100)
  discription                     String?                           @db.VarChar(255)
  email                           String?                           @db.VarChar(255)
  active                          Boolean?                          @default(true)
  role                            Int?
  school                          Int?
  event                           Boolean?
  quota                           Int?                              @default(0)
  quota_used                      Int?                              @default(0)
  zaker                           Int?                              @default(0) @db.TinyInt
  web_image                       String?                           @db.VarChar(255)
  logo                            String?                           @db.VarChar(255)
  name                            String?                           @db.VarChar(200)
  slug                            String?                           @unique(map: "slug_UNIQUE") @db.VarChar(100)
  live_max_duration               Int?                              @default(120)
  teaching_start_date             DateTime?                         @db.DateTime(0)
  web_title                       String?                           @db.VarChar(200)
  percentage_of_attendance        Int?                              @default(50)
  gather_specific_information     Json?
  created_at                      DateTime?                         @default(now()) @db.DateTime(0)
  city_id                         Int?
  out_of_ashtar                   Int?                              @default(0) @db.TinyInt
  enable_reservation              Int?                              @default(0) @db.TinyInt
  enable_question_bank            Int?                              @default(0) @db.TinyInt
  country_id                      Int                               @default(1)
  profit_percentage               Int?                              @default(70)
  ads                             ads[]
  advertise_subjects              advertise_subjects[]
  ashtar_codes                    ashtar_codes[]
  buyers                          buyers[]
  center                          center[]
  codes                           codes[]
  comments                        comments[]
  comments_relations              comments_relations[]
  contracts                       contracts[]
  enrollments                     enrollments[]
  invoice_report                  invoice_report[]
  meeting_details                 meeting_details[]
  point_configurations            point_configurations[]
  promo_codes                     promo_codes[]
  reels                           reels[]
  reservation                     reservation[]
  resources                       resources[]
  reviews                         reviews[]
  sessions                        sessions[]
  sms_logs                        sms_logs[]
  student_point_balances          student_point_balances[]
  subjects                        subjects[]
  subjects_grouping_teachers      subjects_grouping_teachers[]
  teacher_images                  teacher_images[]
  teacher_social_media            teacher_social_media[]
  teacher_versions                teacher_versions[]
  teacher_videos                  teacher_videos[]
  cities                          cities?                           @relation(fields: [city_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_teachers_city_id")
  countries                       countries                         @relation(fields: [country_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_teachers_countries")
  teacher_recommender             teachers_recommendations[]        @relation("teacher_recommender")
  teacher_recommended             teachers_recommendations[]        @relation("teacher_recommended")
  teachers_specific_branches      teachers_specific_branches[]
  teachers_zoom_accounts          teachers_zoom_accounts[]
  transaction_our_teacher_details transaction_our_teacher_details[]
  users                           users[]
  wallets                         wallets[]

  @@index([city_id], map: "fk_teachers_cities1_idx")
  @@index([country_id], map: "fk_teachers_countries_idx")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by the Prisma Client.
model teachers_subjects {
  user    Int?
  subject Int?
  type    String? @db.VarChar(100)

  @@ignore
}

model transactions {
  id               Int     @id @default(autoincrement())
  client_id        Int?
  transaction_date String? @db.VarChar(200)
  type             String? @db.VarChar(255)
  paid             Int?
  amount           Float?
  currency         String? @db.VarChar(100)
  payment          String? @db.VarChar(255)
  ref              String? @db.VarChar(255)
  description      String? @db.VarChar(255)
}

model users {
  id                    Int                     @id @default(autoincrement())
  teacher               Int?
  firstname             String?                 @db.VarChar(255)
  lastname              String?                 @db.VarChar(255)
  email                 String?                 @db.VarChar(255)
  mobile                String?                 @db.VarChar(100)
  role                  Int?
  role_name             String?                 @db.VarChar(100)
  password              String?                 @db.VarChar(255)
  type                  users_type?             @default(admin)
  events                events[]
  history               history[]
  permissions_has_users permissions_has_users[]
  roles_has_users       roles_has_users[]
  teachers              teachers?               @relation(fields: [teacher], references: [id], onDelete: Cascade, map: "users")

  @@index([teacher], map: "teacher")
}

model version {
  id               Int                @id @default(autoincrement())
  v_code           String?            @db.VarChar(45)
  status           String?            @db.VarChar(45)
  message_ar       String?            @db.VarChar(255)
  message_en       String?            @db.VarChar(255)
  created_time     DateTime?          @default(now()) @db.DateTime(0)
  created_by       String?            @db.VarChar(45)
  edit_time        DateTime?          @default(now()) @db.DateTime(0)
  type             version_type?      @default(IOS)
  apple_reviewer   Int?               @default(0) @db.TinyInt
  notes            String?            @db.VarChar(500)
  descriptions     String?            @db.VarChar(700)
  students_version students_version[]
  teacher_versions teacher_versions[]
}

model weeks {
  id           Int     @id @default(autoincrement())
  subject      Int?
  title        String? @db.VarChar(255)
  order        Int?
  type         String? @db.VarChar(255)
  descriptions String? @db.VarChar(255)
}

model weights {
  id          Int           @id @default(autoincrement())
  title       String?       @db.VarChar(255)
  order       Int?
  weight      Int?
  subject     Int?
  module      String?       @db.VarChar(255)
  tag         String?       @db.VarChar(255)
  assignments assignments[]
}

model workflow {
  id              Int     @id @default(autoincrement())
  history_id      Int?
  order           Int?
  title           String? @db.VarChar(255)
  after_per_hours Int?
  description     String? @db.VarChar(500)
  type            String? @db.VarChar(255)
  template        String? @db.VarChar(500)
}

model workflow_steps {
  id          Int     @id @default(autoincrement())
  workflow    Int?
  title       String? @db.VarChar(255)
  type        String? @db.VarChar(255)
  owner       Int?
  description String? @db.VarChar(255)
}

model workflows {
  id          Int     @id @default(autoincrement())
  title       String? @db.VarChar(255)
  description String? @db.VarChar(255)
}

model wprogress {
  id                        String  @id @db.VarChar(255)
  student                   Int?
  system                    String? @db.VarChar(255)
  grade                     String? @db.VarChar(255)
  subject                   String? @db.VarChar(255)
  week_title                String? @db.VarChar(255)
  week_path                 String? @db.VarChar(255)
  right_answers             Int?
  wrong_answers             Int?
  bloom_knowledge_right     Int?
  bloom_knowledge_wrong     Int?
  bloom_application_right   Int?
  bloom_application_wrong   Int?
  bloom_understanding_right Int?
  bloom_understanding_wrong Int?
  bloom_analysis_right      Int?
  bloom_analysis_wrong      Int?
  bloom_creating_right      Int?
  bloom_creating_wrong      Int?
  bloom_evaluation_right    Int?
  bloom_evaluation_wrong    Int?
  dok_recall_right          Int?
  dok_recall_wrong          Int?
  dok_skill_right           Int?
  dok_skill_wrong           Int?
  dok_strategic_right       Int?
  dok_strategic_wrong       Int?
  dok_extended_right        Int?
  dok_extended_wrong        Int?
}

model activities_comments {
  students_id   Int
  activities_id Int
  id            Int        @id @default(autoincrement())
  comment       String     @db.VarChar(500)
  created_at    DateTime?  @default(now()) @db.DateTime(0)
  activities    activities @relation(fields: [activities_id], references: [id], onDelete: Cascade, map: "fk_students_has_activities_activities1")
  students      students   @relation(fields: [students_id], references: [id], onDelete: Cascade, map: "fk_students_has_activities_students1")

  @@index([activities_id], map: "fk_students_has_activities_activities1_idx")
  @@index([students_id], map: "fk_students_has_activities_students1_idx")
}

model activities_likes {
  activities_id Int
  students_id   Int
  created_at    DateTime?  @default(now()) @db.DateTime(0)
  liked         Int?       @default(1) @db.TinyInt
  dislike       Int?       @db.TinyInt
  activities    activities @relation(fields: [activities_id], references: [id], onDelete: Cascade, map: "fk_activities_has_students1_activities1")
  students      students   @relation(fields: [students_id], references: [id], onDelete: Cascade, map: "fk_activities_has_students1_students1")

  @@id([activities_id, students_id])
  @@index([activities_id], map: "fk_activities_has_students1_activities1_idx")
  @@index([students_id], map: "fk_activities_has_students1_students1_idx")
}

model cities {
  id                     Int                      @id @default(autoincrement())
  name                   String                   @unique(map: "name_UNIQUE") @db.VarChar(100)
  location               Unsupported("point")
  country_id             Int?
  name_en                String?                  @db.VarChar(45)
  booklet_stores_details booklet_stores_details[]
  booklets_stores        booklets_stores[]
  center                 center[]
  countries              countries?               @relation(fields: [country_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "FK_country_city")
  reservation            reservation[]
  students               students[]
  teachers               teachers[]
  vendor_branches        vendor_branches[]

  @@index([country_id], map: "FK_country_city_idx")
  @@index([location(length: 32)], map: "location")
}

model comments_likes {
  id         Int      @id @default(autoincrement())
  comment_id Int
  student_id Int
  comments   comments @relation(fields: [comment_id], references: [id], onDelete: Cascade, map: "post_comment_like")
  students   students @relation(fields: [student_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "student_comment_like")

  @@index([comment_id], map: "fk_comments_likes_comments1_idx")
  @@index([student_id], map: "fk_comments_likes_students1_idx")
}

model components {
  id                                                                          Int                          @id @default(autoincrement())
  type                                                                        String?                      @db.VarChar(45)
  icon                                                                        String?                      @db.VarChar(255)
  gif_link                                                                    String?                      @db.VarChar(255)
  name                                                                        String?                      @db.VarChar(255)
  validate                                                                    Int?                         @default(1) @db.TinyInt
  onboarding_component                                                        onboarding_component[]
  parent_children_components_parent_children_components_child_idTocomponents  parent_children_components[] @relation("parent_children_components_child_idTocomponents")
  parent_children_components_parent_children_components_parent_idTocomponents parent_children_components[] @relation("parent_children_components_parent_idTocomponents")
}

model live_rooms {
  id       String  @id @db.VarChar(255)
  lessonId Int
  lessons  lessons @relation(fields: [lessonId], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_live_rooms_lessons1")

  @@index([lessonId], map: "fk_live_rooms_lessons1_idx")
}

model offer_views {
  id         Int      @id @default(autoincrement())
  offer_id   Int
  student_id Int
  offers     offers   @relation(fields: [offer_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_offers_id")
  students   students @relation(fields: [student_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_students_id")

  @@index([offer_id], map: "fk_offer_views_offers1_idx")
  @@index([student_id], map: "fk_offer_views_students1_idx")
}

model offers {
  id                   Int                    @id @default(autoincrement())
  name                 String                 @db.VarChar(100)
  description          String?                @db.VarChar(255)
  expire_date          DateTime?              @db.DateTime(0)
  created_at           DateTime?              @db.DateTime(0)
  percentage           Int
  all                  Int                    @default(1) @db.TinyInt
  vendor_id            Int
  active               Int?                   @default(1) @db.TinyInt
  views                Int?                   @default(0)
  image                String?                @db.VarChar(300)
  offer_views          offer_views[]
  vendors              vendors                @relation(fields: [vendor_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "vendor_id")
  vendor_branch_offers vendor_branch_offers[]
  vendor_offer_codes   vendor_offer_codes[]

  @@index([vendor_id], map: "fk_offers_vendor1_idx")
}

model onboarding_steps {
  id                                                        Int                         @id @default(autoincrement())
  order                                                     Int?                        @unique(map: "order_UNIQUE")
  isActive                                                  Boolean?                    @default(true)
  screen_name                                               String                      @db.VarChar(50)
  duration                                                  Int?                        @default(4)
  branches                                                  branches[]
  category                                                  category[]
  grades                                                    grades[]
  onboarding_component                                      onboarding_component[]
  subjects_grouping                                         subjects_grouping[]
  survey_answers_survey_answers_next_stepToonboarding_steps survey_answers[]            @relation("survey_answers_next_stepToonboarding_steps")
  survey_future_msg_content                                 survey_future_msg_content[]
  survey_questions_answers                                  survey_questions_answers[]
  survey_testimonial                                        survey_testimonial[]
  surveys_surveys_depends_on_stepToonboarding_steps         surveys[]                   @relation("surveys_depends_on_stepToonboarding_steps")
  surveys                                                   surveys[]
  surveys_surveys_previous_stepToonboarding_steps           surveys[]                   @relation("surveys_previous_stepToonboarding_steps")
  user_survey                                               user_survey[]
}

model parent_children_components {
  id                                                          Int         @id @default(autoincrement())
  parent_id                                                   Int?
  child_id                                                    Int?
  components_parent_children_components_child_idTocomponents  components? @relation("parent_children_components_child_idTocomponents", fields: [child_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_child_component_id")
  components_parent_children_components_parent_idTocomponents components? @relation("parent_children_components_parent_idTocomponents", fields: [parent_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_parent_component_id")

  @@index([child_id], map: "fk_child_component_id_idx")
  @@index([parent_id], map: "fk_parent_component_id_idx")
}

model paymob_codes {
  id              Int           @id @default(autoincrement())
  phone_number    String?       @db.VarChar(45)
  email           String?       @db.VarChar(45)
  ashtar_codes_id Int?
  course_code_id  Int?
  ashtar_codes    ashtar_codes? @relation(fields: [ashtar_codes_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_paymob_codes_ashtar_codes1")
  codes           codes?        @relation(fields: [course_code_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_course_code_id")

  @@index([ashtar_codes_id], map: "fk_paymob_codes_ashtar_codes1_idx")
  @@index([course_code_id], map: "fk_paymob_codes_codes1_idx")
}

model post_images {
  id      Int    @id @default(autoincrement())
  image   String @db.VarChar(300)
  post_id Int
  posts   posts  @relation(fields: [post_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "post_image")

  @@index([post_id], map: "fk_post_images_posts1_idx")
}

model post_likes {
  id         Int      @id @default(autoincrement())
  student_id Int
  post_id    Int
  posts      posts    @relation(fields: [post_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "post_likes")
  students   students @relation(fields: [student_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "student_like")

  @@index([post_id], map: "fk_post_likes_posts1_idx")
  @@index([student_id], map: "fk_post_likes_students1_idx")
}

model post_tags {
  id        Int     @id @default(autoincrement())
  post_id   Int
  lesson_id Int
  lessons   lessons @relation(fields: [lesson_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "lessons_id")
  posts     posts   @relation(fields: [post_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "posts_id")

  @@index([lesson_id], map: "fk_post_tags_lessons1_idx")
  @@index([post_id], map: "fk_post_tags_posts1_idx")
}

model posts {
  id                Int                 @id @default(autoincrement())
  description       String?             @db.VarChar(1000)
  link              String?             @db.VarChar(500)
  student_id        Int?
  active            Int?                @default(1) @db.TinyInt
  subject_id        Int?
  status            String?             @default("new") @db.VarChar(45)
  seen              Int?                @default(0)
  date              DateTime?           @db.DateTime(0)
  vimeo_id          String?             @db.VarChar(500)
  publish           Int?                @default(0) @db.TinyInt
  comments          comments[]
  post_images       post_images[]
  post_likes        post_likes[]
  post_tags         post_tags[]
  students          students?           @relation(fields: [student_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "owner_id")
  subjects          subjects?           @relation(fields: [subject_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "subject_id")
  student_post_seen student_post_seen[]

  @@index([student_id], map: "fk_posts_students1_idx")
  @@index([subject_id], map: "fk_posts_subjects1_idx")
}

model promo_codes {
  id                              Int                               @id @default(autoincrement())
  promo_code                      String                            @unique(map: "promo_code_UNIQUE") @db.VarChar(255)
  owner                           Int?
  used                            Int?                              @default(0) @db.TinyInt
  discount                        Int
  reciver                         Int?
  teacher_id                      Int?
  content_id                      Int?
  content_type                    promo_codes_content_type?
  subject_id                      Int?
  zaker                           promo_codes_zaker?
  payments                        payments[]
  owners                          students?                         @relation("promo_code_owner", fields: [owner], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_promo_codes_students1")
  receivers                       students?                         @relation("promo_code_receiver", fields: [reciver], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_promo_codes_students2")
  subjects                        subjects?                         @relation(fields: [subject_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_promo_codes_subject")
  teachers                        teachers?                         @relation(fields: [teacher_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_promo_codes_teacher_id")
  promo_codes_details             promo_codes_details[]
  transaction_our_teacher_details transaction_our_teacher_details[]
  zaker_transactions              zaker_transactions[]

  @@index([owner], map: "fk_promo_codes_students1_idx")
  @@index([reciver], map: "fk_promo_codes_students2_idx")
  @@index([subject_id], map: "fk_promo_codes_subject_idx")
  @@index([teacher_id], map: "fk_promo_codes_teacher_id")
}

model reviews {
  id       Int       @id @default(autoincrement())
  student  Int?
  name     String?   @db.VarChar(90)
  review   String    @db.VarChar(255)
  teacher  Int
  source   String?   @db.VarChar(45)
  students students? @relation(fields: [student], references: [id], map: "fk_reviews_students1")
  teachers teachers  @relation(fields: [teacher], references: [id], onDelete: Cascade, map: "fk_reviews_teachers1")

  @@index([student], map: "fk_reviews_students1_idx")
  @@index([teacher], map: "fk_reviews_teachers1_idx")
}

model student_post_seen {
  id         Int      @id @default(autoincrement())
  student_id Int
  post_id    Int
  posts      posts    @relation(fields: [post_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "post_seen")
  students   students @relation(fields: [student_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "student_seen")

  @@index([post_id], map: "fk_student_post_seen_posts1_idx")
  @@index([student_id], map: "fk_student_post_seen_students1_idx")
}

model submission_details {
  id                Int                @id @default(autoincrement())
  student_id        Int
  submission_id     Int
  question_id       Int
  option_id         Int?
  answer            String?            @db.VarChar(2500)
  correct           Int?
  attachment        String?            @db.VarChar(255)
  grade             Int?
  comment           String?            @db.VarChar(300)
  created_at        DateTime?          @default(now()) @db.DateTime(0)
  updated_at        DateTime?          @default(now()) @db.DateTime(0)
  questions         questions          @relation(fields: [question_id], references: [id], onDelete: Cascade, map: "fk_submission_details_questions1")
  questions_options questions_options? @relation(fields: [option_id], references: [id], onDelete: Cascade, onUpdate: Restrict, map: "fk_submission_details_questions_options1")
  students          students           @relation(fields: [student_id], references: [id], onDelete: Cascade, map: "fk_submission_details_students1")
  submissions       submissions        @relation(fields: [submission_id], references: [id], onDelete: Cascade, map: "fk_submission_details_submissions1")

  @@index([question_id], map: "fk_submission_details_questions1_idx")
  @@index([option_id], map: "fk_submission_details_questions_options1_idx")
  @@index([student_id], map: "fk_submission_details_students1_idx")
  @@index([submission_id], map: "fk_submission_details_submissions1_idx")
}

model survey_answers {
  id                                                          Int                         @id @default(autoincrement())
  answer                                                      String?                     @db.VarChar(45)
  next_step                                                   Int?
  table                                                       survey_answers_table?
  search_query                                                Json?
  include                                                     Json?
  question_id                                                 Int?
  image                                                       String?                     @db.VarChar(255)
  onboarding_steps_survey_answers_next_stepToonboarding_steps onboarding_steps?           @relation("survey_answers_next_stepToonboarding_steps", fields: [next_step], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "next_step_id")
  surveys                                                     surveys?                    @relation(fields: [question_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "question_id_survey")
  survey_future_msg_content                                   survey_future_msg_content[]

  @@index([next_step], map: "next_step_id_idx")
  @@index([question_id], map: "question_id_survey_idx")
}

model surveys {
  id                                                         Int                        @id @default(autoincrement())
  question                                                   String?                    @db.VarChar(255)
  title                                                      String?                    @db.VarChar(45)
  image                                                      String?                    @db.VarChar(255)
  onboarding_step_id                                         Int?
  question_type_id                                           String?                    @db.VarChar(45)
  answer_type_id                                             String?                    @db.VarChar(45)
  previous_step                                              Int?
  depends_on_step                                            Int?
  skippable                                                  Int?                       @default(1) @db.TinyInt
  duration                                                   Int?                       @default(4)
  survey_answers                                             survey_answers[]
  survey_questions_answers                                   survey_questions_answers[]
  onboarding_steps_surveys_depends_on_stepToonboarding_steps onboarding_steps?          @relation("surveys_depends_on_stepToonboarding_steps", fields: [depends_on_step], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "depends_on_step")
  onboarding_steps                                           onboarding_steps?          @relation(fields: [onboarding_step_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "onboarding_step_id")
  onboarding_steps_surveys_previous_stepToonboarding_steps   onboarding_steps?          @relation("surveys_previous_stepToonboarding_steps", fields: [previous_step], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "previous_step_id")

  @@index([onboarding_step_id], map: "onboarding_step_id_idx")
  @@index([depends_on_step], map: "depends_on_step_idx")
  @@index([previous_step], map: "previous_step_id_idx")
}

model teacher_videos {
  id         Int      @id @default(autoincrement())
  teacher_id Int
  video      String?  @db.VarChar(255)
  video_name String   @db.VarChar(100)
  teachers   teachers @relation(fields: [teacher_id], references: [id], onDelete: Cascade, map: "fk_teacher_videos_teachers1")

  @@index([teacher_id], map: "fk_teacher_videos_teachers1_idx")
}

model teachers_zoom_accounts {
  zoom_account  Int
  teacher       Int
  id            Int           @id @default(autoincrement())
  teachers      teachers      @relation(fields: [teacher], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_zoom_accounts_has_teachers_teachers1")
  zoom_accounts zoom_accounts @relation(fields: [zoom_account], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_zoom_accounts_has_teachers_zoom_accounts1")

  @@unique([zoom_account, teacher], map: "unique_index")
  @@index([teacher], map: "fk_zoom_accounts_has_teachers_teachers1_idx")
  @@index([zoom_account], map: "fk_zoom_accounts_has_teachers_zoom_accounts1_idx")
}

model vendor_branch_offers {
  id               Int             @id @default(autoincrement())
  offer_id         Int
  vendor_branch_id Int
  offers           offers          @relation(fields: [offer_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "offer_id")
  vendor_branches  vendor_branches @relation(fields: [vendor_branch_id], references: [id], onDelete: Cascade, map: "vendor_branch_id")

  @@index([offer_id], map: "fk_vendor_branch_offers_offers1_idx")
  @@index([vendor_branch_id], map: "vendor_branche_id_idx")
}

model vendor_branches {
  id                   Int                    @id @default(autoincrement())
  branch_name          String?                @db.VarChar(100)
  city                 Int
  vendor               Int
  location             String?                @db.VarChar(255)
  lat                  String?                @db.VarChar(255)
  long                 String?                @db.VarChar(255)
  vendor_branch_offers vendor_branch_offers[]
  cities               cities                 @relation(fields: [city], references: [id], onDelete: Cascade, map: "city")
  vendors              vendors                @relation(fields: [vendor], references: [id], onDelete: Cascade, map: "vendor")
  vendor_offer_codes   vendor_offer_codes[]

  @@index([city], map: "fk_vendor_branches_cities1_idx")
  @@index([vendor], map: "fk_vendor_branches_vendor1_idx")
}

model vendor_categories {
  id      Int       @id @default(autoincrement())
  name    String    @unique(map: "name_UNIQUE") @db.VarChar(100)
  image   String?   @db.VarChar(300)
  active  Int?      @default(1) @db.TinyInt
  vendors vendors[]
}

model vendor_offer_codes {
  id                  Int              @id @default(autoincrement())
  code                String           @db.VarChar(45)
  student_id          Int?
  offers_id           Int
  is_used             Int?             @db.TinyInt
  created_at          DateTime?        @default(now()) @db.DateTime(0)
  activated_date      DateTime?        @db.DateTime(0)
  vendor_id           Int
  activated_branch_id Int?
  offers              offers           @relation(fields: [offers_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "offers_id")
  students            students?        @relation(fields: [student_id], references: [id], onDelete: Cascade, map: "student_id")
  vendors             vendors          @relation(fields: [vendor_id], references: [id], onDelete: Cascade, map: "vendors_id")
  vendor_branches     vendor_branches? @relation(fields: [activated_branch_id], references: [id], onDelete: Cascade, map: "branch_id")

  @@index([offers_id], map: "fk_vendor_offer_codes_offers1_idx")
  @@index([student_id], map: "fk_vendor_offer_codes_students1_idx")
  @@index([vendor_id], map: "fk_vendor_offer_codes_vendor1_idx")
  @@index([activated_branch_id], map: "fk_vendor_offer_codes_vendor_branches1_idx")
}

model vendors {
  id                 Int                  @id @default(autoincrement())
  name               String               @db.VarChar(100)
  email              String               @db.VarChar(100)
  password           String               @db.VarChar(100)
  phone              String               @db.VarChar(100)
  category           Int
  logo               String?              @db.VarChar(300)
  uid                String               @db.VarChar(100)
  is_online          Int?                 @default(0) @db.TinyInt
  offers             offers[]
  vendor_branches    vendor_branches[]
  vendor_offer_codes vendor_offer_codes[]
  vendor_categories  vendor_categories    @relation(fields: [category], references: [id], onDelete: Cascade, onUpdate: Restrict, map: "category")

  @@index([category], map: "fk_vendor_vendor_categories1_idx")
}

model wallet_transactions {
  id                              Int                              @id @default(autoincrement())
  wallet_id                       Int
  transaction_type                String                           @db.VarChar(45)
  amount                          Float
  transaction_date                DateTime?                        @default(now()) @db.DateTime(0)
  description                     String?                          @db.VarChar(255)
  transaction_id                  Int?
  transaction_details_id          Int?
  transactions_our_teachers       transactions_our_teachers?       @relation(fields: [transaction_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_wallet_transaction")
  transaction_our_teacher_details transaction_our_teacher_details? @relation(fields: [transaction_details_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_wallet_transaction_details")
  wallets                         wallets                          @relation(fields: [wallet_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "wallet_id")

  @@index([transaction_details_id], map: "fk_wallet_transaction_details_idx")
  @@index([transaction_id], map: "fk_wallet_transaction_idx")
  @@index([wallet_id], map: "fk_wallet_transactions_wallets1_idx")
}

model wallets {
  id                  Int                   @id @default(autoincrement())
  balance             Float                 @default(0)
  created_at          DateTime?             @default(now()) @db.DateTime(0)
  updated_at          DateTime?             @default(now()) @db.DateTime(0)
  student_id          Int?
  parent_id           Int?
  teacher_id          Int?
  super_user_id       Int?
  country_id          Int?                  @default(1)
  sms_quota           Int?                  @default(0)
  wallet_transactions wallet_transactions[]
  countries           countries?            @relation(fields: [country_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_wallets_counties")
  parent              parent?               @relation(fields: [parent_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "parents_id")
  students            students?             @relation(fields: [student_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "students_id")
  super_user          super_user?           @relation(fields: [super_user_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "super_users_id")
  teachers            teachers?             @relation(fields: [teacher_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "teachers_id")

  @@index([country_id], map: "fk_wallets_counties_idx")
  @@index([parent_id], map: "fk_wallets_parent1_idx")
  @@index([student_id], map: "fk_wallets_students1_idx")
  @@index([super_user_id], map: "fk_wallets_super_user1_idx")
  @@index([teacher_id], map: "fk_wallets_teachers1_idx")
}

model zaker {
  subject  Int
  branch   Int
  grade    Int
  title    String?  @db.VarChar(255)
  orignal  Int      @default(1) @db.TinyInt
  branches branches @relation(fields: [branch], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_zaker_branches1")
  grades   grades   @relation(fields: [grade], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_zaker_grades1")
  subjects subjects @relation(fields: [subject], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_zaker_subjects1")

  @@id([subject, grade, branch])
  @@index([branch], map: "fk_zaker_branches1_idx")
  @@index([grade], map: "fk_zaker_grades1_idx")
}

model zoom_accounts {
  id                     Int                      @id @default(autoincrement())
  title                  String                   @db.VarChar(50)
  client_secret          String                   @db.VarChar(100)
  client_id              String                   @db.VarChar(100)
  account_id             String                   @db.VarChar(100)
  sdk_id                 String                   @db.VarChar(100)
  sdk_secrit             String                   @db.VarChar(100)
  all                    Int?                     @default(0) @db.TinyInt
  teachers_zoom_accounts teachers_zoom_accounts[]
  zoom_meeting           zoom_meeting[]
}

model zoom_meeting {
  id                 Int                  @id @default(autoincrement())
  uuid               String               @db.VarChar(55)
  password           String               @db.VarChar(45)
  created_at         DateTime?            @default(now()) @db.DateTime(0)
  end_time           DateTime?            @db.DateTime(0)
  status             zoom_meeting_status? @default(start)
  meeting_number     String               @db.VarChar(255)
  encrypted_password String?              @db.VarChar(100)
  zoom_account       Int?
  lessons            lessons[]
  meeting_details    meeting_details[]
  zoom_accounts      zoom_accounts?       @relation(fields: [zoom_account], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_zoom_meeting_zoom_accounts1")

  @@index([zoom_account], map: "fk_zoom_meeting_zoom_accounts1_idx")
}

model onboarding_component {
  id                 Int              @id @default(autoincrement())
  component_id       Int
  onboarding_step_id Int
  order              Int?
  components         components       @relation(fields: [component_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_component_id")
  onboarding_steps   onboarding_steps @relation(fields: [onboarding_step_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_onboarding_step_id")

  @@index([component_id], map: "fk_component_id_idx")
  @@index([onboarding_step_id], map: "fk_onboarding_step_id_idx")
}

model booklets_stores {
  id                     Int                      @id @default(autoincrement())
  name                   String?                  @db.VarChar(200)
  phone                  String?                  @db.VarChar(100)
  address                String?                  @db.VarChar(200)
  city                   Int?
  icon                   String?                  @db.VarChar(200)
  location               String?                  @db.VarChar(500)
  booklet_stores_details booklet_stores_details[]
  cities                 cities?                  @relation(fields: [city], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_city_booklet")

  @@index([city], map: "fk_city_booklet_idx")
}

model subjects_grouping {
  id                        Int                         @id @default(autoincrement())
  title                     String?                     @db.VarChar(45)
  showed_title              String?                     @db.VarChar(45)
  icon                      String?                     @db.VarChar(255)
  next_step                 Int?
  devision_subject_grouping devision_subject_grouping[]
  onboarding_steps          onboarding_steps?           @relation(fields: [next_step], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk3_next_step")
  subjects_grouping_grades  subjects_grouping_grades[]

  @@index([next_step], map: "fk3_next_step_idx")
}

model subjects_grouping_grades {
  id                         Int                          @id @default(autoincrement())
  grade_id                   Int
  subjects_grouping_id       Int
  devision_id                Int?
  modarsina_mirror           modarsina_mirror[]
  reservation                reservation[]
  resources                  resources[]
  subjects                   subjects[]
  devision                   devision?                    @relation(fields: [devision_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "devision_id")
  grades                     grades                       @relation(fields: [grade_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "grade_id")
  subjects_grouping          subjects_grouping            @relation(fields: [subjects_grouping_id], references: [id], onUpdate: NoAction, map: "subjects_grouping_id")
  subjects_grouping_teachers subjects_grouping_teachers[]

  @@index([grade_id], map: "grade_id_idx")
  @@index([subjects_grouping_id], map: "subjects_grouping_id_idx")
  @@index([devision_id], map: "devision_id_idx")
}

model survey_future_msg_content {
  id               Int               @id @default(autoincrement())
  survey_answer_id Int?
  positive_message String?           @db.VarChar(255)
  next_step        Int?
  step_id          Int?
  onboarding_steps onboarding_steps? @relation(fields: [next_step], references: [id], onDelete: Cascade, map: "fk_nextStep")
  survey_answers   survey_answers?   @relation(fields: [survey_answer_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_survey_future_msg_content_survey_answers1")

  @@index([next_step], map: "fk_nextStep_idx")
  @@index([survey_answer_id], map: "fk_survey_future_msg_content_survey_answers1_idx")
}

model survey_questions_answers {
  id               Int               @id @default(autoincrement())
  question_id      Int?
  answer_id        Int?
  device_id        String?           @db.VarChar(45)
  step_id          Int?
  created_at       DateTime?         @default(now()) @db.DateTime(0)
  updated_at       DateTime?         @default(now()) @db.DateTime(0)
  text_answer      String?           @db.VarChar(255)
  user_survey      user_survey?      @relation(fields: [device_id], references: [device_id], onDelete: Cascade, onUpdate: NoAction, map: "device_id")
  surveys          surveys?          @relation(fields: [question_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_question_id")
  onboarding_steps onboarding_steps? @relation(fields: [step_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_step_id")

  @@index([device_id], map: "device_id_idx")
  @@index([question_id], map: "fk_question_id_idx")
  @@index([step_id], map: "fk_step_id_idx")
}

model survey_testimonial {
  id               Int               @id @default(autoincrement())
  icon             String?           @db.VarChar(255)
  title            String?           @db.VarChar(45)
  description      String?           @db.VarChar(255)
  next_step        Int?
  stars            Float?
  onboarding_steps onboarding_steps? @relation(fields: [next_step], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_next_step_id")

  @@index([next_step], map: "fk_next_step_id_idx")
}

model teacher_social_media {
  id         Int      @id @default(autoincrement())
  teacher_id Int
  type       String   @db.VarChar(50)
  link       String   @db.VarChar(200)
  icon       String?  @db.VarChar(255)
  teachers   teachers @relation(fields: [teacher_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_teacher_social_media_teachers1")

  @@index([teacher_id], map: "fk_teacher_social_media_teachers1_idx")
}

model teacher_versions {
  id         Int       @id @default(autoincrement())
  zaker      Int?      @db.TinyInt
  teacher_id Int?
  version_id Int
  subject_id Int?
  version    version   @relation(fields: [version_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "verision_blocked")
  subjects   subjects? @relation(fields: [subject_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "verision_subject")
  teachers   teachers? @relation(fields: [teacher_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "verision_teachers")

  @@index([version_id], map: "verision_blocked_idx")
  @@index([subject_id], map: "verision_subject_idx")
  @@index([teacher_id], map: "verision_teachers_idx")
}

model user_survey {
  id                       Int                        @id @default(autoincrement())
  device_id                String                     @unique(map: "device_id_UNIQUE") @db.VarChar(45)
  user_id                  Int?
  step_id                  Int?
  created_at               DateTime?                  @default(now()) @db.DateTime(0)
  user_surveycol1          DateTime?                  @default(now()) @db.DateTime(0)
  survey_questions_answers survey_questions_answers[]
  onboarding_steps         onboarding_steps?          @relation(fields: [step_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "step_id")
  students                 students?                  @relation(fields: [user_id], references: [id], map: "user_id")

  @@index([step_id], map: "step_id_idx")
  @@index([user_id], map: "user_id_idx")
}

model invitations {
  id      Int  @id @default(autoincrement())
  sender  Int?
  reciver Int?
}

model subjects_grouping_teachers {
  id                          Int                      @id @default(autoincrement())
  subjects_grouping_grades_id Int
  teacher_id                  Int
  subjects_grouping_grades    subjects_grouping_grades @relation(fields: [subjects_grouping_grades_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "subjects_grouping_grades")
  teachers                    teachers                 @relation(fields: [teacher_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_teacher_id")

  @@index([subjects_grouping_grades_id], map: "subject_grouping_grades_idx")
  @@index([teacher_id], map: "teacher_grouping_idx")
}

model students_version {
  id         Int       @id @default(autoincrement())
  student_id Int?
  version_id Int?
  students   students? @relation(fields: [student_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "student_version")
  version    version?  @relation(fields: [version_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "version_student")

  @@index([student_id], map: "student_version_idx")
  @@index([version_id], map: "version_student_idx")
}

model booklet_stores_details {
  id              Int             @id @default(autoincrement())
  booklet_id      Int
  phone           String?         @db.VarChar(45)
  address         String?         @db.VarChar(300)
  city            Int?
  location        String?         @db.VarChar(500)
  booklets_stores booklets_stores @relation(fields: [booklet_id], references: [id], onDelete: Cascade, map: "booklet_id")
  cities          cities?         @relation(fields: [city], references: [id], onDelete: NoAction, map: "booklet_city")

  @@index([booklet_id], map: "bookletId_idx")
  @@index([city], map: "cities_idx")
}

model teachers_recommendations {
  recommender_id       Int
  recommended_id       Int
  id                   Int      @id @default(autoincrement())
  teacher_recommenders teachers @relation("teacher_recommender", fields: [recommender_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "recommender_id")
  teacher_recommendeds teachers @relation("teacher_recommended", fields: [recommended_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "recommended_id")

  @@index([recommender_id], map: "fk_teachers_recommendations_teachers1_idx")
  @@index([recommended_id], map: "fk_teachers_recommendations_teachers2_idx")
}

model tiers {
  id              Int         @id @default(autoincrement())
  title           String      @unique(map: "title_UNIQUE") @db.VarChar(45)
  price           Float       @db.Float
  number_of_codes Int
  contracts       contracts[]
}

model contracts {
  id                        Int       @id @default(autoincrement())
  teacher_id                Int
  tier_id                   Int
  start_date                DateTime  @db.DateTime(0)
  end_date                  DateTime  @db.DateTime(0)
  is_active                 Int       @default(1) @db.TinyInt
  countdown                 Int?
  super_deactivation        DateTime? @db.DateTime(0)
  super_deactivations_count Int?      @default(0)
  teachers                  teachers  @relation(fields: [teacher_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "teacher_contract")
  tiers                     tiers     @relation(fields: [tier_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "tier_contract")

  @@index([teacher_id], map: "fk_contracts_teachers1_idx")
  @@index([tier_id], map: "fk_contracts_tiers1_idx")
}

model bundles {
  id                    Int                     @id @default(autoincrement())
  name                  String                  @db.VarChar(100)
  price                 Int
  online_price          Int?
  image                 String?                 @db.VarChar(400)
  codes                 codes[]
  payment_details       payment_details[]
  subject_bundle        subject_bundle[]
  transactions_subjects transactions_subjects[]
}

model center {
  id               Int           @id @default(autoincrement())
  city_id          Int
  title            String?       @db.VarChar(45)
  location_details String?       @db.VarChar(45)
  teacher_id       Int
  cities           cities        @relation(fields: [city_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_center_city_id")
  teachers         teachers      @relation(fields: [teacher_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_center_teacher_id")
  reservation      reservation[]

  @@index([city_id], map: "fk_center_cities1_idx")
  @@index([teacher_id], map: "fk_center_teachers1_idx")
}

model devision {
  id                        Int                         @id @default(autoincrement())
  title                     String?                     @db.VarChar(45)
  branch_id                 Int?
  branches                  branches?                   @relation(fields: [branch_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "branch_dev")
  devision_subject_grouping devision_subject_grouping[]
  subjects_grouping_grades  subjects_grouping_grades[]

  @@index([branch_id], map: "branch_dev_idx")
}

model devision_subject_grouping {
  id                  Int                @id @default(autoincrement())
  devision_id         Int?
  subject_grouping_id Int?
  devision            devision?          @relation(fields: [devision_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "devId")
  subjects_grouping   subjects_grouping? @relation(fields: [subject_grouping_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "gradeId")

  @@index([devision_id], map: "devid_idx")
  @@index([subject_grouping_id], map: "groupId_idx")
}

model old_ashtar_enrollments {
  id              Int       @id @unique(map: "id_UNIQUE") @default(autoincrement())
  student         Int?
  branch          String?   @db.VarChar(100)
  grade           String?   @db.VarChar(100)
  code            String?   @db.VarChar(50)
  enrollment_date DateTime? @db.DateTime(0)
  points          Int?
  active          Boolean?
  balance         Int?
  group           Int?
  comments        String?   @db.VarChar(500)
  old_student     Int?
  students        students? @relation(fields: [student], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_old_ashtar_enrollments_students1")

  @@index([branch], map: "subject")
  @@index([grade], map: "teacher_id")
  @@index([student], map: "fk_old_ashtar_enrollments_students1")
}

model payment_details {
  id              Int                         @id @default(autoincrement())
  payments_id     Int
  subjects_id     Int?
  bundles_id      Int?
  term_id         Int?
  detailed_amount Decimal?                    @db.Decimal(10, 2)
  child_id        Int?
  child_type      payment_details_child_type?
  bundles         bundles?                    @relation(fields: [bundles_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_payment_bundles_id")
  subjects        subjects?                   @relation(fields: [subjects_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_pyment_subjects_id")
  terms           terms?                      @relation(fields: [term_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_pyment_terms_id")
  payments        payments                    @relation(fields: [payments_id], references: [id], onDelete: Cascade, map: "fk_payments_id")

  @@index([child_id], map: "fk_child_id")
  @@index([bundles_id], map: "fk_payment_details_bundles1_idx")
  @@index([subjects_id], map: "fk_payment_details_subjects1_idx")
  @@index([term_id], map: "fk_payment_details_terms1_idx")
  @@index([payments_id], map: "fk_payment_payments_idx")
}

model reservation {
  id                         Int                       @id @default(autoincrement())
  name                       String                    @db.VarChar(45)
  phone                      String                    @db.VarChar(45)
  parent_phone               String                    @db.VarChar(45)
  grade_id                   Int
  division                   String                    @db.VarChar(45)
  city_id                    Int?
  school                     String?                   @db.VarChar(45)
  second_language            String?                   @db.VarChar(45)
  subject_grouping_grades_id Int?
  teacher_id                 Int?
  center_id                  Int?
  student_id                 Int?
  center                     center?                   @relation(fields: [center_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_reservation_center_id")
  cities                     cities?                   @relation(fields: [city_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_reservation_city_id")
  grades                     grades                    @relation(fields: [grade_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_reservation_grade_id")
  students                   students?                 @relation(fields: [student_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_reservation_student_id")
  subjects_grouping_grades   subjects_grouping_grades? @relation(fields: [subject_grouping_grades_id], references: [id], onUpdate: NoAction, map: "fk_reservation_subject_grouping_grade_id")
  teachers                   teachers?                 @relation(fields: [teacher_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_reservation_teachers1")

  @@index([center_id], map: "fk_reservation_center1_idx")
  @@index([city_id], map: "fk_reservation_cities1_idx")
  @@index([grade_id], map: "fk_reservation_grades1_idx")
  @@index([student_id], map: "fk_reservation_students1_idx")
  @@index([subject_grouping_grades_id], map: "fk_reservation_subjects_grouping_grades1_idx")
  @@index([teacher_id], map: "fk_reservation_teachers1_idx")
}

model student_subscribtions {
  id                  Int                  @id @default(autoincrement())
  student_id          Int
  subscribtion_id     Int?
  grade_id            Int
  enroll_date         DateTime             @default(now()) @db.DateTime(0)
  expiry_date         DateTime?            @db.DateTime(0)
  question_bank       Int?                 @default(0) @db.TinyInt
  grade_plan_id       Int?
  active              Int?                 @default(1) @db.TinyInt
  grades              grades               @relation(fields: [grade_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_student_subscribtions_grades1")
  students            students             @relation(fields: [student_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_student_subscribtions_students1")
  zaker_subscribtions zaker_subscribtions? @relation(fields: [subscribtion_id], references: [id], onDelete: NoAction, map: "fk_student_subscribtions_zaker_subscribtions1")
  zaker_grade_plans   zaker_grade_plans?   @relation(fields: [grade_plan_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_zaker_grade_plans")

  @@index([grade_id], map: "fk_student_subscribtions_grades1_idx")
  @@index([student_id], map: "fk_student_subscribtions_students1_idx")
  @@index([subscribtion_id], map: "fk_student_subscribtions_zaker_subscribtions1_idx")
  @@index([grade_plan_id], map: "fk_zaker_grade_plans_idx")
}

model subject_bundle {
  id         Int      @id @default(autoincrement())
  subject_id Int
  bundle_id  Int
  term_id    Int?
  bundles    bundles  @relation(fields: [bundle_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_bundle_subject_bundle_id")
  subjects   subjects @relation(fields: [subject_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_bundle_subject_subject_id")
  terms      terms?   @relation(fields: [term_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_bundle_subject_term_id")

  @@index([bundle_id], map: "fk_subject_bundle_bundles1_idx")
  @@index([subject_id], map: "fk_subject_bundle_subjects1_idx")
  @@index([term_id], map: "fk_subject_bundle_terms1_idx")
}

model subjects_terms {
  id           Int      @id @default(autoincrement())
  subject_id   Int
  term_id      Int
  price        Int?
  price_online Int?
  subjects     subjects @relation(fields: [subject_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_subject_terms_subject_id")
  terms        terms    @relation(fields: [term_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_subject_terms_term_id")

  @@index([subject_id], map: "fk_subjects_terms_subjects1_idx")
  @@index([term_id], map: "fk_subjects_terms_terms1_idx")
}

model teachers_specific_branches {
  id         Int      @id @default(autoincrement())
  teacher_id Int
  branch_id  Int
  branches   branches @relation(fields: [branch_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "branch_idFK")
  teachers   teachers @relation(fields: [teacher_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_teachers_specific_branches_teachers1")

  @@index([teacher_id], map: "fk_teachers_specific_branches_teachers1_idx")
  @@index([branch_id], map: "fk_teachers_specific_branches_branches1_idx")
}

model terms {
  id                 Int                  @id @default(autoincrement())
  title              String               @db.VarChar(45)
  branch_id          Int
  showed_title       String?              @db.VarChar(45)
  identify_arrange   Int?
  ashtar_enrollments ashtar_enrollments[]
  enrollments        enrollments[]
  lessons            lessons[]
  payment_details    payment_details[]
  payments           payments[]
  resources          resources[]
  subject_bundle     subject_bundle[]
  subjects_terms     subjects_terms[]
  branches           branches             @relation(fields: [branch_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "branch_term")
  zaker_grade_plans  zaker_grade_plans[]

  @@unique([branch_id, identify_arrange], map: "unique_identifiier_term_arrange")
  @@index([branch_id], map: "fk_terms_branches1_idx")
}

model transactions_our_teachers {
  id                              Int                               @id @default(autoincrement())
  total_invoice                   Float?
  actual_paid                     Float?
  student_id                      Int?
  admin_id                        Int?
  payment_id                      Int?
  created_at                      DateTime?                         @default(now()) @db.DateTime(0)
  active                          Int?                              @default(1) @db.TinyInt
  buyers                          buyers[]
  coupons                         coupons[]
  enrollments                     enrollments[]
  transaction_our_teacher_details transaction_our_teacher_details[]
  super_user                      super_user?                       @relation(fields: [admin_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_admin_mod")
  payments                        payments?                         @relation(fields: [payment_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_payments_transaction_our_teacher")
  students                        students?                         @relation(fields: [student_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_transactions_our_teachers_student_id")
  transactions_subjects           transactions_subjects[]
  wallet_transactions             wallet_transactions[]

  @@index([admin_id], map: "fk_admin_super_id_idx")
  @@index([payment_id], map: "fk_payments_transaction_our_teacher_idx")
  @@index([student_id], map: "fk_transactions_our_teachers_students1_idx")
}

model transactions_subjects {
  id                        Int                        @id @default(autoincrement())
  bundle_id                 Int?
  subjects_id               Int?
  transaction_id            Int?
  bundles                   bundles?                   @relation(fields: [bundle_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_transactions_subjects_bundle_id")
  subjects                  subjects?                  @relation(fields: [subjects_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_transactions_subjects_subject_id")
  transactions_our_teachers transactions_our_teachers? @relation(fields: [transaction_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_transactions_subjects_transaction_id")

  @@index([bundle_id], map: "fk_transactions_subjects_bundles1_idx")
  @@index([subjects_id], map: "fk_transactions_subjects_subjects1_idx")
  @@index([transaction_id], map: "fk_transactions_subjects_transactions_our_teachers1_idx")
}

model zaker_subscribtion_prices {
  id                     Int                  @id @default(autoincrement())
  grades_id              Int?
  zaker_subscribtions_id Int?
  sub_term_price         Int?
  sub_year_price         Int?
  question_bank          Int?                 @default(0) @db.TinyInt
  actual_sub_term_price  Int?                 @default(600)
  actual_sub_year_price  Int?                 @default(1000)
  payments               payments[]
  grades                 grades?              @relation(fields: [grades_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_sub_grades_id")
  zaker_subscribtions    zaker_subscribtions? @relation(fields: [zaker_subscribtions_id], references: [id], onDelete: Cascade, map: "fk_zaker_subs_id")
  zaker_transactions     zaker_transactions[]

  @@index([grades_id], map: "fk_table1_grades1_idx")
  @@index([zaker_subscribtions_id], map: "fk_table1_zaker_subscribtions1_idx")
}

model zaker_subscribtions {
  id                        Int                         @id @default(autoincrement())
  title                     String?                     @db.VarChar(100)
  number_of_courses         Int?
  all_courses               Int?                        @default(0) @db.TinyInt
  price_of_one_subject      Int?                        @default(100)
  student_subscribtions     student_subscribtions[]
  zaker_plans               zaker_plans[]
  zaker_subscribtion_prices zaker_subscribtion_prices[]
}

model zaker_transactions {
  id                           Int                        @id @default(autoincrement())
  actual_paid                  Float?
  total_invoice                Float?
  term                         Int?                       @db.TinyInt
  zaker_subscribtion_prices_id Int?
  students_id                  Int?
  code                         String?                    @db.VarChar(100)
  created_at                   DateTime?                  @default(now()) @db.DateTime(0)
  expiry_date                  DateTime?                  @db.DateTime(0)
  admin_id                     Int?
  sold_price                   Float?
  currancy                     String?                    @default("EGP") @db.VarChar(45)
  promo_code_id                Int?
  grade_plan_id                Int?
  active                       Int?                       @default(1) @db.TinyInt
  ashtar_enrollments           ashtar_enrollments[]
  super_user                   super_user?                @relation(fields: [admin_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_admin_id")
  zaker_grade_plans            zaker_grade_plans?         @relation(fields: [grade_plan_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_grade_plan_id")
  promo_codes                  promo_codes?               @relation(fields: [promo_code_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_zaker_transaction_promocode")
  students                     students?                  @relation(fields: [students_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_zaker_studetns_id")
  zaker_subscribtion_prices    zaker_subscribtion_prices? @relation(fields: [zaker_subscribtion_prices_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_zaker_subs_prices_id")

  @@index([admin_id], map: "fk_admin_zaker_id_idx")
  @@index([students_id], map: "fk_zaker_transactions_students1_idx")
  @@index([zaker_subscribtion_prices_id], map: "fk_zaker_transactions_zaker_subscribtion_prices1_idx")
  @@index([grade_plan_id], map: "fk_grade_plan_id_idx")
  @@index([promo_code_id], map: "fk_zaker_transaction_promocode_idx")
}

model advertise_details {
  id                 Int                           @id @default(autoincrement())
  advertise_id       Int
  enrolled_in        advertise_details_enrolled_in
  child_id           Int
  activity_id        Int?
  lesson_id          Int?
  activities         activities?                   @relation(fields: [activity_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_advertise_activity")
  advertise_subjects advertise_subjects            @relation(fields: [advertise_id], references: [id], onDelete: Cascade, map: "fk_advertise_details_advertise_teachers_id")
  lessons            lessons?                      @relation(fields: [lesson_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_advertise_lesson")

  @@index([activity_id], map: "fk_activities_details_adverties_idx")
  @@index([advertise_id], map: "fk_adverties_details_idx")
  @@index([lesson_id], map: "fk_lessons_details_adverties_idx")
}

model advertise_subjects {
  id                              Int                               @id @default(autoincrement())
  title                           String?                           @db.VarChar(60)
  online_price                    Int
  price                           Int
  advertise_description           String                            @db.VarChar(255)
  start_date                      DateTime                          @db.DateTime(0)
  end_date                        DateTime                          @db.DateTime(0)
  subject_id                      Int
  teacher_id                      Int
  active                          Int?                              @default(1) @db.TinyInt
  is_online                       Int?                              @default(1) @db.TinyInt
  advertise_details               advertise_details[]
  subjects                        subjects                          @relation(fields: [subject_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_subject_id")
  teachers                        teachers                          @relation(fields: [teacher_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_teachers_id")
  transactions_advertise_subjects transactions_advertise_subjects[]

  @@index([subject_id], map: "fk_subjects_adverties_idx")
  @@index([teacher_id], map: "fk_teachers_adverties_idx")
}

model transactions_advertise_subjects {
  id                    Int                @id @default(autoincrement())
  advertise_subjects_id Int
  student_id            Int
  advertise_subjects    advertise_subjects @relation(fields: [advertise_subjects_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk.advertis_subjects")
  students              students           @relation(fields: [student_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_student_id")

  @@index([advertise_subjects_id], map: "fk_transaction_advertiese_idx")
  @@index([student_id], map: "fk_transactions_advertise_subjects_students1_idx")
}

model zaker_grade_post_handlers {
  id         Int       @id @default(autoincrement())
  email      String    @db.VarChar(45)
  grade_id   Int
  subject_id Int?
  grades     grades    @relation(fields: [grade_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_grade_post")
  subjects   subjects? @relation(fields: [subject_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_subject_posts_handlers")

  @@index([subject_id], map: "fk_subject_posts_handlers_idx")
  @@index([grade_id], map: "fk_grade_posts_handlers_idx")
}

model branch_countries {
  id         Int       @id @default(autoincrement())
  branch_id  Int
  country_id Int
  branches   branches  @relation(fields: [branch_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "FK_branch")
  countries  countries @relation(fields: [country_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "FK_country")

  @@index([branch_id], map: "fk_branches_idx")
  @@index([country_id], map: "fk_country_idx")
}

model emulator_devices {
  id          Int       @id @default(autoincrement())
  emulator_id Int
  deviceId    String?   @db.VarChar(255)
  deviceName  String?   @db.VarChar(45)
  createdAt   DateTime? @default(now()) @db.Timestamp(0)
  emulators   emulators @relation(fields: [emulator_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_emulator")

  @@index([emulator_id], map: "fk_emulator_idx")
}

model emulators {
  id               Int                @id @default(autoincrement())
  network          String             @unique(map: "network_UNIQUE") @db.VarChar(255)
  emulator_devices emulator_devices[]
}

model family_subscription {
  id              Int            @id @default(autoincrement())
  discount        Float?
  number_of_codes Int?
  ashtar_codes    ashtar_codes[]
}

model apple_package {
  id            Int             @id @default(autoincrement())
  name          String?         @db.VarChar(100)
  package_id    String          @db.VarChar(100)
  package_price package_price[]
  subjects      subjects[]
}

model buyer_old {
  id                          Int       @id @default(autoincrement())
  student                     Int?
  subject                     Int?
  teacher                     Int?
  code                        String?   @db.VarChar(50)
  enrollment_date             DateTime? @db.DateTime(0)
  points                      Int?
  active                      Boolean?  @default(true)
  balance                     Int?
  group                       Int?
  activity                    String?   @db.VarChar(255)
  activity_id                 Int?
  comments                    String?   @db.VarChar(500)
  section_id                  Int?
  lesson_id                   Int?
  type                        String?   @db.VarChar(45)
  duration                    Int?
  expired_date                DateTime? @db.DateTime(0)
  transactions_our_teacher_id Int?
  assignment_id               Int?

  @@index([student], map: "fk_buyers_students1_idx")
  @@index([subject], map: "subject")
  @@index([teacher], map: "teacher_id")
}

model coupons_old {
  id                          Int       @id @default(autoincrement())
  title                       String?   @db.VarChar(255)
  subject                     Int?
  teacher                     Int?
  value                       Decimal?  @db.Decimal(10, 0)
  used                        Boolean?
  owner                       Int?
  issued_date                 DateTime? @db.DateTime(0)
  expiry_date                 DateTime? @db.DateTime(0)
  redeem_date                 DateTime? @db.DateTime(0)
  activity                    String?   @db.VarChar(100)
  activity_id                 Int?
  comment                     String?   @db.VarChar(255)
  section_id                  Int?
  lesson_id                   Int?
  type                        String?   @db.VarChar(45)
  duration                    Int?
  created_at                  DateTime? @default(now()) @db.DateTime(0)
  limit                       Int?
  active                      Int?      @default(1) @db.TinyInt
  transactions_our_teacher_id Int?
  assignment_id               Int?

  @@index([lesson_id], map: "fk_coupons_lessons1_idx")
  @@index([section_id], map: "fk_coupons_sections1_idx")
  @@index([subject], map: "fk_coupons_subjects1_idx")
  @@index([owner], map: "owner")
}

model language {
  id            Int     @id @default(autoincrement())
  title         String  @db.VarChar(45)
  country_code  String? @db.VarChar(45)
  language_code String  @db.VarChar(45)
}

model package_price {
  id               Int           @id @default(autoincrement())
  price            Int
  apple_package_id Int
  country_id       Int
  apple_package    apple_package @relation(fields: [apple_package_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_package_price_apple_package1")
  countries        countries     @relation(fields: [country_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_package_price_countries")

  @@index([apple_package_id], map: "fk_package_price_apple_package1_idx")
  @@index([country_id], map: "fk_package_price_countries_idx")
}

model teacher_images {
  id         Int      @id @default(autoincrement())
  teacher_id Int
  image      String?  @db.VarChar(200)
  teachers   teachers @relation(fields: [teacher_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_teacher_images_teachers1")

  @@index([teacher_id], map: "fk_teacher_images_teachers1_idx")
}

model transaction_our_teacher_details {
  id                        Int                                             @id @default(autoincrement())
  transactionId             Int
  teacherId                 Int
  studentId                 Int?
  promocodeId               Int?
  description               String                                          @db.VarChar(500)
  originalPrice             Decimal                                         @db.Decimal(10, 2)
  actualPaid                Decimal                                         @db.Decimal(10, 2)
  contentId                 Int
  paidFor                   transaction_our_teacher_details_paidFor
  createdAt                 DateTime                                        @db.DateTime(0)
  is_online_transaction     Int?                                            @default(1) @db.TinyInt
  description_en            String?                                         @db.VarChar(500)
  active                    Int?                                            @default(1) @db.TinyInt
  payment_method            transaction_our_teacher_details_payment_method?
  profit_percentage         Int?                                            @default(70)
  profit                    Float?                                          @default(0)
  promo_codes               promo_codes?                                    @relation(fields: [promocodeId], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_promocode_details")
  students                  students?                                       @relation(fields: [studentId], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_student_details")
  teachers                  teachers                                        @relation(fields: [teacherId], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_teacher_details")
  transactions_our_teachers transactions_our_teachers                       @relation(fields: [transactionId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_transaction_details")
  wallet_transactions       wallet_transactions[]

  @@index([promocodeId], map: "fk_promocode_details_idx")
  @@index([studentId], map: "fk_student_details_idx")
  @@index([teacherId], map: "fk_teacher_details_idx")
  @@index([transactionId], map: "fk_transaction_details_idx")
}

model invoice_report {
  id         Int      @id @default(autoincrement())
  teacher_id Int
  link       String   @db.VarChar(455)
  teachers   teachers @relation(fields: [teacher_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_invoice_report_teacher")

  @@index([teacher_id], map: "teacher_invoice")
}

model point_configurations {
  id                     Int                      @id @default(autoincrement())
  activity_id            Int?
  assignment_id          Int?
  lesson_id              Int?
  points                 Int                      @default(10)
  is_active              Boolean?                 @default(true)
  created_at             DateTime?                @default(now()) @db.Timestamp(0)
  updated_at             DateTime?                @default(now()) @db.Timestamp(0)
  teacher_id             Int                      @default(337)
  section_id             Int?
  teachers               teachers                 @relation(fields: [teacher_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_point_configurations_teacher")
  activities             activities?              @relation(fields: [activity_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "point_configurations_ibfk_1")
  assignments            assignments?             @relation(fields: [assignment_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "point_configurations_ibfk_2")
  lessons                lessons?                 @relation(fields: [lesson_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "point_configurations_ibfk_3")
  sections               sections?                @relation(fields: [section_id], references: [id], onDelete: Cascade, onUpdate: Restrict, map: "point_section_fk")
  student_point_earnings student_point_earnings[]

  @@unique([activity_id, assignment_id, lesson_id], map: "unique_content_grade")
  @@index([teacher_id], map: "fk_point_configurations_teacher")
  @@index([activity_id], map: "idx_activity_id")
  @@index([assignment_id], map: "idx_assignment_id")
  @@index([lesson_id], map: "idx_lesson_id")
  @@index([section_id], map: "point_section_fk_idx")
}

model student_point_balances {
  id                   Int       @id @default(autoincrement())
  teacher_id           Int
  student_id           Int
  grade_id             Int
  total_points         Int?      @default(0)
  available_coins      Int?      @default(0)
  total_coins_earned   Int?      @default(0)
  total_coins_redeemed Int?      @default(0)
  created_at           DateTime? @default(now()) @db.Timestamp(0)
  updated_at           DateTime? @default(now()) @db.Timestamp(0)
  teachers             teachers  @relation(fields: [teacher_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "student_point_balances_ibfk_1")
  students             students  @relation(fields: [student_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "student_point_balances_ibfk_2")
  grades               grades    @relation(fields: [grade_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "student_point_balances_ibfk_3")

  @@unique([student_id, teacher_id, grade_id], map: "unique_student_teacher_grade")
  @@index([grade_id], map: "idx_grade_id")
  @@index([student_id], map: "idx_student_id")
  @@index([teacher_id], map: "idx_teacher_id")
  @@index([total_points], map: "idx_total_points")
}

model student_point_earnings {
  id                     Int                  @id @default(autoincrement())
  student_id             Int
  point_configuration_id Int
  points_earned          Int
  grade_id               Int
  created_at             DateTime?            @default(now()) @db.Timestamp(0)
  students               students             @relation(fields: [student_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "student_point_earnings_ibfk_1")
  point_configurations   point_configurations @relation(fields: [point_configuration_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "student_point_earnings_ibfk_2")
  grades                 grades               @relation(fields: [grade_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "student_point_earnings_ibfk_3")

  @@unique([student_id, point_configuration_id], map: "unique_student_content")
  @@index([created_at], map: "idx_created_at")
  @@index([grade_id], map: "idx_grade_id")
  @@index([student_id], map: "idx_student_id")
  @@index([point_configuration_id], map: "point_configuration_id")
}

model zaker_grade_plans {
  id                    Int                     @id @default(autoincrement())
  grades_id             Int?
  active                Int?                    @db.TinyInt
  zaker_plans_id        Int?
  end_date              DateTime?               @db.DateTime(0)
  most_pobular          Int?                    @db.TinyInt
  price                 Int?
  term_id               Int?
  payments              payments[]
  student_subscribtions student_subscribtions[]
  terms                 terms?                  @relation(fields: [term_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_grade_plans_terms")
  grades                grades?                 @relation(fields: [grades_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_grades")
  zaker_plans           zaker_plans?            @relation(fields: [zaker_plans_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_zaker_plans")
  zaker_transactions    zaker_transactions[]

  @@index([grades_id], map: "fk_grades_idx")
  @@index([zaker_plans_id], map: "fk_zaker_plans")
  @@index([term_id], map: "fk_grade_plans_terms_idx")
}

model zaker_plan_upgrades {
  id           Int         @id @default(autoincrement())
  from_plan_id Int
  to_plan_id   Int
  upgrade      Int?        @default(0) @db.TinyInt
  fromPlan     zaker_plans @relation("FromPlanRelation", fields: [from_plan_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_from_plan")
  toPlan       zaker_plans @relation("ToPlanRelation", fields: [to_plan_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_to_plan")

  @@index([from_plan_id], map: "fk_from_plan")
  @@index([to_plan_id], map: "fk_to_plan")
}

model zaker_plans {
  id                   Int                      @id @default(autoincrement())
  title                String?                  @db.VarChar(60)
  active               Int?                     @default(1) @db.TinyInt
  duration             Int?                     @default(30)
  features             Json?
  subscribtions_id     Int
  term_arrange         Int?
  description          String?                  @db.VarChar(300)
  question_bank        Int?                     @default(0) @db.TinyInt
  zaker                Int?                     @default(0) @db.TinyInt
  live_session         Int?                     @default(0) @db.TinyInt
  question_bank_enroll Int?                     @default(0) @db.TinyInt
  zaker_enroll         Int?                     @default(0) @db.TinyInt
  live_session_enroll  Int?                     @default(0) @db.TinyInt
  zaker_grade_plans    zaker_grade_plans[]
  upgradesFromThisPlan zaker_plan_upgrades[]    @relation("FromPlanRelation")
  upgradesToThisPlan   zaker_plan_upgrades[]    @relation("ToPlanRelation")
  upgradesV2From       zaker_plan_upgrades_v2[] @relation("FromPlanRelation")
  upgradesV2To         zaker_plan_upgrades_v2[] @relation("ToPlanRelation")
  zaker_subscribtions  zaker_subscribtions      @relation(fields: [subscribtions_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_subscriptions")

  @@index([subscribtions_id], map: "fk_subscriptions_idx")
}

model calendar_events {
  id               Int                @id @default(autoincrement())
  lesson_id        Int?
  date             DateTime?          @db.Date
  grade_id         Int?
  grades           grades?            @relation(fields: [grade_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_event_grade")
  lessons          lessons?           @relation(fields: [lesson_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_event_lesson")
  student_calendar student_calendar[]

  @@index([lesson_id], map: "fk_event_lesson_idx")
  @@index([grade_id], map: "fk_event_grade_idx")
}

model student_calendar {
  id                Int              @id @default(autoincrement())
  student_id        Int
  calendar_event_id Int?
  date              DateTime?        @db.Date
  students          students         @relation(fields: [student_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_calendar_student")
  calendar_events   calendar_events? @relation(fields: [calendar_event_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_calender_event")

  @@index([student_id], map: "fk_calendar_student")
  @@index([calendar_event_id], map: "fk_calender_event")
}

model private_subjects_students {
  id         Int       @id @default(autoincrement())
  student_id Int?
  subject_id Int?
  students   students? @relation(fields: [student_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_private_student_id")
  subjects   subjects? @relation(fields: [subject_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_private_subject_id")

  @@index([student_id], map: "fk_private_student_id")
  @@index([subject_id], map: "fk_private_subject_id_idx")
}

model sms_logs {
  id              Int                  @id @default(autoincrement())
  session_id      Int
  student_id      Int
  teacher_id      Int
  parent_phone    String               @db.VarChar(20)
  message_content String               @db.Text
  send_status     sms_logs_send_status
  error_message   String?              @db.Text
  cost            Decimal              @db.Decimal(10, 4)
  country_code    String               @db.VarChar(5)
  sent_at         DateTime             @db.DateTime(0)
  created_at      DateTime             @default(now()) @db.DateTime(0)
  updated_at      DateTime             @default(now()) @db.DateTime(0)
  parts           Int                  @default(1)
  sessions        sessions             @relation(fields: [session_id], references: [id], onDelete: Cascade, map: "fk_sms_logs_session")
  students        students             @relation(fields: [student_id], references: [id], onDelete: Cascade, map: "fk_sms_logs_student")
  teachers        teachers             @relation(fields: [teacher_id], references: [id], onDelete: Cascade, map: "fk_sms_logs_teacher")

  @@index([parent_phone], map: "idx_parent_phone")
  @@index([send_status], map: "idx_send_status")
  @@index([sent_at], map: "idx_sent_at")
  @@index([session_id], map: "idx_session_id")
  @@index([student_id], map: "idx_student_id")
  @@index([teacher_id], map: "idx_teacher_id")
}

model ashtar_codes_old {
  id          Int       @id @default(autoincrement())
  code        String?   @db.VarChar(50)
  title       String?   @db.VarChar(255)
  grade       Int?
  value       Decimal?  @db.Decimal(10, 0)
  used        Boolean?  @default(false)
  owner       Int?
  issued_date DateTime? @db.DateTime(6)
  expiry_date DateTime? @db.DateTime(6)
  redeem_date DateTime? @db.DateTime(6)
  teacher     Int?
  comment     String?   @db.VarChar(255)
  active      Int       @default(1) @db.TinyInt
  expiry_days Int?
  created_at  DateTime? @default(now()) @db.DateTime(0)
  admin_id    Int?
  discount    Int?

  @@index([admin_id], map: "fk_admin_idx")
  @@index([grade], map: "fk_ashtar_codes_grades1_idx")
  @@index([owner], map: "fk_ashtar_codes_students1_idx")
  @@index([teacher], map: "fk_ashtar_codes_teachers1_idx")
  @@index([discount], map: "fk_family_subscription_idx")
}

model ashtar_enrollments_old {
  id              Int       @id @unique(map: "id_UNIQUE") @default(autoincrement())
  student         Int?
  grade           Int?
  code            String?   @db.VarChar(50)
  enrollment_date DateTime? @db.DateTime(0)
  points          Int?
  active          Boolean?  @default(true)
  balance         Int?
  group           Int?
  comments        String?   @db.VarChar(500)
  old_student     Int?
  terms_id        Int?
  transactions_id Int
  payment_types   String    @default("codes") @db.VarChar(50)
  question_bank   Int?      @default(0) @db.TinyInt
  subject_id      Int?
  expiry_date     DateTime? @db.DateTime(0)

  @@index([subject_id], map: "fk_ashtar_enrollments_subjects1_idx")
  @@index([terms_id], map: "fk_ashtar_enrollments_terms1_idx")
  @@index([transactions_id], map: "fk_ashtar_enrollments_zaker_transactions1_idx")
  @@index([student], map: "subscribers")
  @@index([grade], map: "teacher_id")
}

model enrollments_old {
  id                         Int       @id @default(autoincrement())
  student                    Int?
  subject                    Int?
  teacher                    Int?
  code                       String?   @db.VarChar(50)
  enrollment_date            DateTime? @db.DateTime(0)
  points                     Int?
  active                     Boolean?  @default(true)
  balance                    Int?
  group                      Int?
  comments                   String?   @db.VarChar(500)
  old_student                Int?
  online                     Int?      @db.TinyInt
  deactivater                String?   @db.VarChar(45)
  role_deactivater           String?   @db.VarChar(45)
  term_id                    Int?
  our_teacher_transaction_id Int?
  expiry_date                DateTime? @db.DateTime(0)
  archive                    Int?      @default(0) @db.TinyInt
  withdraw                   Int?      @default(0) @db.TinyInt

  @@index([student], map: "fk_enrollments_students1_idx")
  @@index([term_id], map: "fk_enrollments_terms1_idx")
  @@index([our_teacher_transaction_id], map: "fk_enrollments_transactions_our_teachers1_idx")
  @@index([teacher, subject], map: "idx_teacher_subject")
  @@index([subject], map: "subject")
}

model old_enrollments {
  id                         Int       @id @default(autoincrement())
  student                    Int?
  subject                    Int?
  teacher                    Int?
  code                       String?   @db.VarChar(50)
  enrollment_date            DateTime? @db.DateTime(0)
  points                     Int?
  active                     Boolean?  @default(true)
  balance                    Int?
  group                      Int?
  comments                   String?   @db.VarChar(500)
  old_student                Int?
  online                     Int?      @db.TinyInt
  deactivater                String?   @db.VarChar(45)
  role_deactivater           String?   @db.VarChar(45)
  term_id                    Int?
  our_teacher_transaction_id Int?
  expiry_date                DateTime? @db.DateTime(0)
  archive                    Int?      @default(0) @db.TinyInt
  withdraw                   Int?      @default(0) @db.TinyInt

  @@index([student], map: "fk_enrollments_students1_idx")
  @@index([term_id], map: "fk_enrollments_terms1_idx")
  @@index([our_teacher_transaction_id], map: "fk_enrollments_transactions_our_teachers1_idx")
  @@index([teacher, subject], map: "idx_teacher_subject")
  @@index([subject], map: "subject")
}

model payments_old {
  id                            Int                            @id @default(autoincrement())
  student                       Int?
  grade_id                      Int?
  amount                        Decimal?                       @db.Decimal(10, 2)
  payment_date                  DateTime?                      @db.DateTime(6)
  status                        payments_old_status?
  ref                           String                         @unique(map: "ref_UNIQUE") @db.VarChar(150)
  description                   String?                        @db.VarChar(255)
  InvoiceId                     String?                        @db.VarChar(255)
  payment_method                String?                        @default("fawry") @db.VarChar(45)
  terms_id                      Int?
  zaker_subscribtions_prices_id Int?
  is_wallet                     Boolean?                       @default(false)
  partial_wallet_amount         Decimal?                       @default(0.00) @db.Decimal(10, 2)
  payment_paid_for              payments_old_payment_paid_for? @default(subject)
  originalPrice                 Decimal?                       @db.Decimal(10, 2)
  question_bank                 Int?                           @default(0) @db.TinyInt
  currency                      String?                        @default("EGP") @db.VarChar(45)
  promo_code_id                 Int?
  uuid                          Bytes?                         @unique(map: "uuid_UNIQUE") @db.Binary(16)
  grade_plan_id                 Int?

  @@index([grade_id], map: "fk_payments_grades1_idx")
  @@index([promo_code_id], map: "fk_payments_promo_code_idx")
  @@index([terms_id], map: "fk_payments_terms1_idx")
  @@index([zaker_subscribtions_prices_id], map: "fk_payments_zaker_subscribtion_prices1_idx")
  @@index([payment_method], map: "idex_payment_type")
  @@index([student], map: "students_payments")
}

model posts_old {
  id          Int       @id @default(autoincrement())
  description String?   @db.VarChar(1000)
  link        String?   @db.VarChar(500)
  student_id  Int?
  active      Int?      @default(1) @db.TinyInt
  subject_id  Int?
  status      String?   @default("new") @db.VarChar(45)
  seen        Int?      @default(0)
  date        DateTime? @db.DateTime(0)
  vimeo_id    String?   @db.VarChar(500)
  publish     Int?      @default(0) @db.TinyInt

  @@index([student_id], map: "fk_posts_students1_idx")
  @@index([subject_id], map: "fk_posts_subjects1_idx")
}

model student_subscribtions_old {
  id              Int       @id @default(autoincrement())
  student_id      Int
  subscribtion_id Int
  grade_id        Int
  enroll_date     DateTime  @default(now()) @db.DateTime(0)
  expiry_date     DateTime? @db.DateTime(0)
  question_bank   Int?      @default(0) @db.TinyInt

  @@index([grade_id], map: "fk_student_subscribtions_grades1_idx")
  @@index([student_id], map: "fk_student_subscribtions_students1_idx")
  @@index([subscribtion_id], map: "fk_student_subscribtions_zaker_subscribtions1_idx")
}

model transaction_our_teacher_details_old {
  id                    Int                                                 @id @default(autoincrement())
  transactionId         Int
  teacherId             Int
  studentId             Int
  promocodeId           Int?
  description           String                                              @db.VarChar(500)
  originalPrice         Decimal                                             @db.Decimal(10, 2)
  actualPaid            Decimal                                             @db.Decimal(10, 2)
  contentId             Int
  paidFor               transaction_our_teacher_details_old_paidFor
  createdAt             DateTime                                            @db.DateTime(0)
  is_online_transaction Int?                                                @default(1) @db.TinyInt
  description_en        String?                                             @db.VarChar(500)
  active                Int?                                                @default(1) @db.TinyInt
  payment_method        transaction_our_teacher_details_old_payment_method?
  profit_percentage     Int?                                                @default(70)
  profit                Float?                                              @default(0)

  @@index([promocodeId], map: "fk_promocode_details_idx")
  @@index([studentId], map: "fk_student_details_idx")
  @@index([teacherId], map: "fk_teacher_details_idx")
  @@index([transactionId], map: "fk_transaction_details_idx")
}

model zaker_transactions_old {
  id                           Int       @id @default(autoincrement())
  actual_paid                  Float?
  total_invoice                Float?
  term                         Int?      @db.TinyInt
  zaker_subscribtion_prices_id Int?
  students_id                  Int?
  code                         String?   @db.VarChar(100)
  created_at                   DateTime? @default(now()) @db.DateTime(0)
  expiry_date                  DateTime? @db.DateTime(0)
  admin_id                     Int?
  sold_price                   Float?
  currancy                     String?   @default("EGP") @db.VarChar(45)
  promo_code_id                Int?

  @@index([admin_id], map: "fk_admin_zaker_id_idx")
  @@index([promo_code_id], map: "fk_zaker_transaction_promocode_idx")
  @@index([students_id], map: "fk_zaker_transactions_students1_idx")
  @@index([zaker_subscribtion_prices_id], map: "fk_zaker_transactions_zaker_subscribtion_prices1_idx")
}

model modarsina_mirror {
  id                        Int                       @id @default(autoincrement())
  subject_id                Int?
  grade_id                  Int?
  branch_id                 Int?
  original                  Int?                      @default(1) @db.TinyInt
  subject_grouping_grade_id Int?
  branches                  branches?                 @relation(fields: [branch_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_branch_mirror")
  grades                    grades?                   @relation(fields: [grade_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_grade_mirror")
  subjects_grouping_grades  subjects_grouping_grades? @relation(fields: [subject_grouping_grade_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_subject_grouping_grade_mirror")
  subjects                  subjects?                 @relation(fields: [subject_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_subject_mirror")

  @@index([branch_id], map: "fk_branch_mirror_idx")
  @@index([grade_id], map: "fk_grade_mirror_idx")
  @@index([subject_grouping_grade_id], map: "fk_subject_grouping_grade_mirror_idx")
  @@index([subject_id], map: "fk_subject_mirror_idx")
}

model zaker_plan_upgrades_v2 {
  id           Int         @id @default(autoincrement())
  from_plan_id Int
  to_plan_id   Int
  upgrade      Int?        @default(0) @db.TinyInt
  fromPlanV2   zaker_plans @relation("FromPlanRelation", fields: [from_plan_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_from_plan_v2")
  toPlanV2     zaker_plans @relation("ToPlanRelation", fields: [to_plan_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_to_plan_v2")

  @@index([from_plan_id], map: "fk_from_plan_v2")
  @@index([to_plan_id], map: "fk_to_plan_v2")
}

model course_group_submit_app {
  id              Int             @id @default(autoincrement())
  student_id      Int?
  course_group_id Int?
  approved        Int?            @default(0) @db.TinyInt
  courses_groups  courses_groups? @relation(fields: [course_group_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_course_group_app")
  students        students?       @relation(fields: [student_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_students_app")

  @@index([student_id], map: "fk_students_app")
  @@index([course_group_id], map: "fk_course_group_app_idx")
}

model meeting_details {
  id           Int           @id @default(autoincrement())
  lesson_id    Int?
  teacher_id   Int?
  meeting_id   Int?
  created_at   DateTime?     @default(now()) @db.DateTime(0)
  end_at       DateTime?     @db.DateTime(0)
  lessons      lessons?      @relation(fields: [lesson_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_zoom_lesson_id")
  zoom_meeting zoom_meeting? @relation(fields: [meeting_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_zoom_meeting_id")
  teachers     teachers?     @relation(fields: [teacher_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_zoom_teacher_id")

  @@index([lesson_id, meeting_id], map: "fk_zoom_lesson_id_idx")
  @@index([meeting_id], map: "fk_zoom_meeting_id_idx")
  @@index([teacher_id], map: "fk_zoom_teacher_id")
}

model promo_codes_details {
  id            Int                               @id @default(autoincrement())
  promo_code_id Int?
  content_id    Int?
  content_type  promo_codes_details_content_type?
  promo_codes   promo_codes?                      @relation(fields: [promo_code_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_promo_code_id")

  @@index([promo_code_id], map: "fk_promo_code_id_idx")
}

model bunny_video_events {
  id             Int      @id @default(autoincrement())
  bunny_video_id Int
  event_type     String   @db.VarChar(64)
  bunny_event_id String?  @unique(map: "bunny_event_id_UNIQUE") @db.VarChar(128)
  status_before  String?  @db.VarChar(32)
  status_after   String?  @db.VarChar(32)
  payload        Json
  received_at    DateTime @default(now()) @db.DateTime(0)

  @@index([bunny_video_id], map: "fk_bunny_video_events_videos1_idx")
}

model bunny_videos {
  id                  Int                 @id @default(autoincrement())
  bunny_guid          String?             @unique(map: "bunny_guid_UNIQUE") @db.VarChar(64)
  library_id          String?             @db.VarChar(64)
  title               String?             @db.VarChar(255)
  source_path         String?             @db.VarChar(2000)
  source_url          String?             @db.VarChar(2000)
  status              bunny_videos_status @default(pending)
  retry_count         Int                 @default(0)
  last_error          String?             @db.Text
  duration_ms         Int?
  width               Int?
  height              Int?
  size_bytes          BigInt?
  thumbnail_url       String?             @db.VarChar(900)
  iframe_url          String?             @db.VarChar(900)
  hls_url             String?             @db.VarChar(900)
  uploader_type       String?             @db.VarChar(32)
  uploader_id         Int?
  created_at          DateTime?           @default(now()) @db.Timestamp(0)
  updated_at          DateTime?           @default(now()) @db.Timestamp(0)
  fetched_at          DateTime?           @db.DateTime(0)
  ready_at            DateTime?           @db.DateTime(0)
  failed_at           DateTime?           @db.DateTime(0)
  bunny_video_id      String?             @db.VarChar(100)
  bunny_collection_id String?             @db.VarChar(100)

  @@index([status])
  @@index([uploader_type, uploader_id], map: "bunny_videos_uploader_idx")
}

enum lives_status {
  live
  created
  finished
  pause
}

enum assignments_questions_type {
  Specific
  Random
}

enum zoom_meeting_status {
  start
  progress
  end
  waiting
}

enum survey_answers_table {
  grades
  branches
  category
  zaker
  subjects_grouping_grades
  survey_testimonial
  survey_future_msg_content
  subjects_grouping
}

enum advertise_details_enrolled_in {
  lesson
  activity
}

enum payments_status {
  PENDING
  PAID
  UNPAID
}

enum promo_codes_content_type {
  activity
  lesson
  section
  advertise
  assignment
}

enum version_type {
  Android
  IOS
  Huawei
}

enum transaction_our_teacher_details_paidFor {
  subject
  activity
  lesson
  section
  assignment
  advertise
}

enum promo_codes_zaker {
  zaker
  question_bank
}

enum attendance_type {
  absent
  attend
  attended_online
}

enum attendance_attendance_method {
  manual
  excel_import
  auto_online
}

enum sms_logs_send_status {
  success
  failed
}

enum subjects_category {
  free
  myStudents
  general
}

enum payments_old_status {
  PENDING
  PAID
  UNPAID
}

enum transaction_our_teacher_details_old_paidFor {
  subject
  activity
  lesson
  section
  assignment
  advertise
}

enum payments_old_payment_paid_for {
  subject
  activity
  lesson
  section
  advertise
  assignment
}

enum transaction_our_teacher_details_old_payment_method {
  fawry
  paymob
  fatoora
  thawani
  code
  ashtar_wallet
}

enum transaction_our_teacher_details_payment_method {
  fawry
  paymob
  fatoora
  thawani
  code
  ashtar_wallet
  applePay
}

enum students_admin_reviewer {
  reached
  attempted
  not_attempted
}

enum promo_codes_details_content_type {
  activity
  lesson
  section
  advertise
  assignment
}

enum payment_details_child_type {
  lesson
  section
  activity
  assignment
}

enum users_type {
  admin
  forum_admin
  live_admin
}

enum payments_payment_paid_for {
  subject
  activity
  lesson
  section
  advertise
  assignment
  multi_direct_content
}

enum bunny_videos_status {
  pending
  fetching
  processing
  ready
  failed
  deleted
}
